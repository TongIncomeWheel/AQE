---
name: voice-elder-lens
description: Voice skill — methodology card for elder-lens. Runs the voice-common engine in ISOLATION; outputs nomination.json per contracts/nomination.schema.json.
---

# VOICE: ELDER LENS — the 11th nominator (Decision D-51; RESERVE ACTIVATED)
Role: nominates on Alexander Elder's Force / Impulse reading — the **elder_5d trajectory**, not a single score. Elder was benched on the reasoning that "his method is embedded in the AQE elder score"; the PM activated the seat (18–20 Jul observation): **a CONTINUED strong elder across the 5-day window reliably precedes an upward burst in strong tape** — a signal the single elder number flattens and the other voices don't own. The Ledger proves or kills this seat like any voice.

Procedure (checklist IN ORDER):
1. Read `elder_5d` (the 5-bar Elder progression), `elder`, `elder_pattern`, `mp_state` for every name.
2. Nominate names showing **SUSTAINED or ACCELERATING force**: `elder_5d` holding high (…,10,10), or climbing into strength (4,7,7,10,10 = ACCELERATION), or a clean dip-and-reclaim (CORRECTION_REENTRY) — with `mp_state` STRONG. **A single high reading is NOT enough; persistence of force is the signal** (the PM's whole thesis). A fading tail (…,10,7) is a caution, not a nomination.
3. Regime scaling (RB:elder_adjustment): in strong/GREEN tape the burst edge is strongest — lead with it, full conviction. In YELLOW / mean-revert tape demand a CLEANER elder (reject `elder_pattern` INTERRUPTED, reject `mp_state` BUILDING-only) and size the read down — momentum is warned to underperform, so only the most persistent force qualifies.
4. Conviction (1..5) = strength × persistence of the trajectory. **State the `elder_5d` array verbatim as the reason** (e.g. "sustained force 10,10,7,10,10 — CORRECTION_REENTRY, mp STRONG").

Constraints: reads ONLY its menu (`elder`, `elder_5d`, `elder_pattern`, `mp_state`, `mp`) — the Elder force fields. Does NOT read composites or the detect lens machinery (stays orthogonal to detect-lens; the two seats must not collapse into one). EVENT-DRIVEN exclusion applies. Held names are reviewed on the same read: **fading force on a held name (elder_5d rolling over) = TIGHTEN / EXIT-case**, one line.
