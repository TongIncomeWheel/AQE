# S5 — CHALLENGE + WEATHER (judgment; 3 agents, AFTER the tally, never before)

**Why after:** weather and challenge must react to what the swarm produced without ever
having steered it (D-4 weather-not-gate; D-97 challenge-sees-tally).

**5a. Rogers — the challenge seat.** One isolated agent: its card + the tally (names,
counts, seats, convictions) + the universe rows for those names. Returns
`challenge.json` — entries on CROWDING / CERTAINTY / TIMING + the Catalyst Check. Severity
`note`/`flag` only, NEVER block. Barred from arguing against a stop, bracket, or gate.

**5b. Crown — the NOW weather.** NOT an LLM opinion: the crown file's own
`read_me_first` + `the_call` are relayed VERBATIM into `weather.json.crown` (the artifact
already wrote the four blocks — headline / why / so-what / what-would-change-it — in plain
English; relaying beats re-generating). Its `expression_family` + `size_multiplier` ride as
context. If `crown_absent`, this block says exactly that.

**5c. Druckenmiller — the NEXT weather.** One isolated agent: its (now grounded, 27-principle)
card + the GLOBAL blocks (regime/intermarket/macro_weather/srm/thematic). Returns the
18-month-lean macro brief: cross-asset read, risk-on/off with the regime caveat stated
plainly, sector head/tailwinds for the deliberation set, sizing tone. → `weather.json.druckenmiller`.

**Pairing (PM ruling 2026-08-11):** crown = NOW, druckenmiller = NEXT. Read back to back in
the plan, never merged into one voice.

**Output.** `data/pma/DATE/challenge.json` + `weather.json`
(contract: `contracts/pma/weather.schema.json`). Neither gates anything.
