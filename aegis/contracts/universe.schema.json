{
 "$schema": "https://json-schema.org/draft/2020-12/schema",
 "title": "Daily Universe File",
 "x_version": "1.0.0",
 "x_note": "The one universe file every voice reads (BL-011). Produced by universe_screen.py from the AQE working read; menu-slimmed per voice at spawn. Validated before any voice reads it.",
 "type": "object",
 "required": ["date", "names"],
 "properties": {
  "date": {"type": "string", "format": "date"},
  "criteria": {"type": "object", "description": "the screen params applied (RB:universe.screen) — provenance"},
  "count": {"type": "integer"},
  "names": {
   "type": "array",
   "description": "the screened universe — the ONLY names voices nominate from",
   "items": {
    "type": "object",
    "required": ["ticker"],
    "properties": {"ticker": {"type": "string"}}
   }
  },
  "near_misses": {
   "type": "array",
   "description": "D-37: names just outside the screen (RB:universe.near_miss) — SURFACED for PM/committee revival, NOT nominated",
   "items": {
    "type": "object",
    "required": ["ticker"],
    "properties": {"ticker": {"type": "string"}, "near_miss_reason": {"type": "string"}}
   }
  }
 }
}
