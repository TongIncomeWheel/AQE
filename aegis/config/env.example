# Copy to .env on the PC. NEVER commit .env. One secret, one line, one place.
FMP_API_KEY=
ALPACA_API_KEY=
ALPACA_SECRET_KEY=
IBKR_ACCOUNT=
IBKR_GATEWAY=https://localhost:5000/v1/api
TIGER_MCP_URL=
GITHUB_PAT=   # only if a session needs push access; prefer pasting per-session
