{
 "name": "aegis-core",
 "version": "1.5.2",
 "description": "Aegis process and orchestration — loops, desks, order path and tooling. Includes /pma (Premarket Analysis committee, v5.3 CONDUCTOR: snapshot-every-tick, verified-read seat spawn, two-wave vote, four admission doors, tool-built R2 digest, executive-form brief: map + scoreboard architecture, forms not essays, registrar validation on arrival, data-steward + brief-writer workers, PM-LENS independent lane, signal-strength purity gate, stamp-verified pre-sliced packets, brief always lands). 1.5.2: dynCap single-writer fix (recompute_dyncap.py writes the full ledger; premarket step 8 reads, never recomputes). 1.5.1: re-synced contracts/voice_menus.json, pma_pipeline.py and emit_packets.py against the 2026-09-05 AQE voice-packet-spec build (18 new export fields routed onto seat menus; chart-pattern fields excluded as a checked build-time invariant). Voices ship separately in aegis-voices (same kernel_build).",
 "kernel_build": "cf837be",
 "layer": "process"
}
