# DECISIONS LOG
Every ruling that changes the constitution or rulebook. Newest first. Format: ID · date · decision · what it retires.

> **STANDING PM DIRECTIVE (18 Jul 2026) — anti-spaghetti.** Hold true to the architecture principles (constitution §3 laws 2, 4, 10; D-8). When a need arises, the default is to COMPLETE or CORRECT an existing structure, or build a genuinely new agent/skill — NOT to overlayer prose, flags, or shims on top of what exists. Every change still names what it retires. If a change would add a fourth way to do something that already has three, stop and consolidate instead. This directive governs how every decision below is implemented.

| ID | Date | Decision (PM ruling) | Retires / reverses |
|---|---|---|---|
| D-87 | 2026-07-28 | **Every held-book write now pushes to GitHub in the SAME session that made it — not just post-market — closing a gap found while writing the plain-language storage confirmation the PM asked for (PM: "confirm again in plain MD what it writes, save it in the kernel so it's basically self understanding, these 2 things have to go hand in hand").** By direct inspection: `git_sync.py` was called zero times from `premarket` or `market_hours` — only `post_market` ever pushed. Each scheduled run is its own fresh session with its own git checkout (no shared disk, no shared process), so premarket's AQE-refresh and stop-update writes (Operations 4/5) and market-hours' fill updates were landing only on that session's local disk and nowhere else — gone the moment the session ended, invisible to every other session including tomorrow's post-market carry-forward. Fixed: premarket now runs `git_sync.py` immediately after Operation 5; market-hours runs it right after any fill-triggered update (skipped on a quiet cycle — this is per-fill, not per-poll). `skills/print-trade-journal/SKILL.md` gains a STORAGE section — a plain table of which session pushes when — placed in the SAME file as the write-operations table so the two can't drift apart, and a new MUST-NOT-DO line: never end a session having written the journal without pushing it. Premarket step 0's preflight text corrected — it previously (wrongly) said the GitHub token was only needed for post-market's commit. | The false assumption that only post-market's write needed to reach GitHub; premarket step 0's stale claim that `GITHUB_PAT` is unneeded outside post-market |
| D-86 | 2026-07-28 | **The held-book write taxonomy becomes its own skill, `print-trade-journal` (reviving the retired external skill's name, kernel-native this time) — one place documenting every write to the journal across the day, instead of the rule being scattered and re-explained inside post-market, premarket, and market-hours separately (PM: "I want it to still carry the same name, and make it as a skill").** New `skills/print-trade-journal/SKILL.md` — invoked inline, not spawned as an agent (none of this needs judgement, it is a fixed sequence of tool calls) — tables five operations (Execution, Carry-forward, Metrics, AQE refresh, Stop update), who runs each and when, and which fields each is and is not allowed to touch. States the rule once: a data-quality problem NEVER blocks a write, it always writes and gets a named entry in `review_flags` instead. Closes a real gap found by direct inspection: `stop_match` had a schema slot but nothing ever computed it, and nothing surfaced a mismatch to the PM — the count sat buried in `metrics.stop_audit` with no escalation. New `stop-update` operation in `tools/held_book_refresh.py` writes `trailing_stop.py`'s fresh floor into `stop_reference` and recomputes `stop_match` against the broker's live value; a MISMATCH right after a recompute is EXPECTED (order not staged yet) and is flagged, not treated as an error. `review_flags` (new top-level journal array: ticker, type, detail, severity, since) is idempotent per (ticker, condition) within a day's file — a re-run refreshes the existing entry rather than duplicating it, and a flag is actively removed once the underlying condition resolves (e.g. a mismatch clears the moment a later check finds MATCH), so the list reflects current state, not a growing log. Folded into the existing 10:00 summary (high-severity flags shown by name; medium/low as counts) — no new PM-facing surface. Also fixed a pre-existing bug found while touching this file: `tools/morning_summary.py` was reading the journal from `data/eod/DATE/aegis_journal_DATE.json`, which is not where post-market writes it (`data/journal/aegis_journal_DATE.json`) — corrected, so the journal section of the summary will actually populate instead of reading CHECK/? every day. | Post-market/premarket/market-hours each separately explaining the held-book write rule inline; a `stop_match` field with no producer and no escalation; `morning_summary.py`'s wrong journal path | 
| D-85 | 2026-07-28 | **The journal becomes a real portfolio snapshot, and the held-book loop between post-market and premarket is now explicit (PM: "the portfolio check figures should be inside the journal so that's a proper snapshot of the portfolio, not just list of positions... it should hold all the data on position level from AQE on the held book, this ensures that the next write, once AQE is run in premarket, the procedure also updates the file with new levels, not overwrite").** Two findings first, by direct inspection, matching the D-84 pattern of checking before building: `contracts/journal.schema.json` already had a REQUIRED `metrics` key that nothing had ever populated, and no tool anywhere computed exposure, leverage, NAV-β, or sector concentration — post-market step 3's old text described machinery that, like the D-84 scorer finding, was never built. **New `tools/held_book_refresh.py`** (deterministic, law 4, selftest-verified) — two operations on `open_positions[].aqe_snapshot` / `aqe_snapshot_as_of` only, never the execution-truth fields: `carry-forward` (post-market step 2, before anything else reads the file) pulls each held ticker's most recent AQE snapshot forward from the prior journal that has one, preserving its TRUE as_of date, never re-stamped to look fresher; `refresh` (premarket step 4, right after the AQE pull) merges TODAY's export fields into the same journal file post-market wrote hours earlier — the update, not overwrite, the PM asked for. A name absent from today's export keeps its stale snapshot rather than being blanked. The named field set (`AQE_SNAPSHOT_FIELDS`, includes `bracket` so `trailing_stop.py`'s `aqe_operative_stop` now sources from this single merged journal, not a second in-memory export read) is a fixed trimmed set, matching the D-81 discipline against a blind 97-field byte-copy. **New `tools/portfolio_metrics.py`** (deterministic, law 4, selftest-verified, reuses `tools/calculators/var_parametric.py` for VaR — not a second implementation) computes gross exposure, leverage, net-beta-dollar, NAV-β, sector concentration, and stop audit (MATCH/MISMATCH/MISSING, already per-position) from whatever `aqe_snapshot` data is currently attached, and writes the result into the journal's existing `metrics` key — one file, one snapshot, no separate metrics file. **The loop, explicitly:** post-market always runs BEFORE premarket pulls AQE each day, so post-market's own portfolio metrics are always computed from AQE data refreshed at an EARLIER premarket run — never same-day, and this is by design, not a bug: `computed_from_aqe_dated` and `mixed_vintage` state this honestly rather than pretending the numbers are current. A position with no snapshot at all (never yet refreshed) is EXCLUDED from leverage/beta/sector/VaR and named in `excluded_no_aqe_data`, never defaulted to zero; a position with no live mark falls back to entry cost basis, flagged `unmarked`, never a fabricated price. `contracts/journal.schema.json` gains `mark_price`, `aqe_snapshot`, `aqe_snapshot_as_of` per position and a documented `metrics` shape. | Post-market step 3's aspirational "metrics file" text (no tool ever implemented it); the journal as a bare execution-truth list with no market-data context on held names; any implicit assumption that post-market's portfolio metrics reflect same-day AQE data (they reflect the most recent premarket refresh, stated honestly) |
| D-84 | 2026-07-28 | **Google Drive retired as the journal/PTJ/archive store — GitHub is the ONE book of record — and the Pipeline Ledger files IMMEDIATELY after premarket's committee, not at the next post-market cycle (PM: "if we already do in GitHub then we should just use GitHub as the storage, it also gives more flexibility in CRUD for Janitor... both files are json right? that means we remove google drive dependency, which is ok since this means the system is truly portable within one git repo... i think we file immediately after premarket, simply because that is the point of time, and it eventually feeds into intraday monitoring").** Three parts. **(1) Storage.** `data/journal/aegis_journal_YYYY-MM-DD.json` (git-committed via `tools/git_sync.py`) is now the SOLE journal/PTJ store — D-67/D-68's separate Drive write of the identical JSON (`aegis_trade_journal_[DATE]_PTJ.json`, folder `15PR74ws_kTXTqCcEfRGga_jjHrMvbCEM`) is retired: it was a duplicate of a file already version-controlled in git, with no upside and a real cost (D-69's whole Drive-verification apparatus existed only to check the duplicate landed). `tools/archive_ledger.py`'s archive (`aegis_trade_journal_ARCHIVE_master.json`) moves the same way, to `data/persistent/aegis_trade_journal_ARCHIVE_master.json` — this also retires the modifiedTime-latest-duplicate guessing D-68 flagged as a known limitation, since git overwrites in place and keeps real history. `tools/daily_flow_audit.py`'s Operations layer now reads `git_sync_result.json` (the `git push` acknowledgement) instead of `ptj_drive_check.json`/`tools/drive_ptj_check.py` (retired, left on disk unused) — a `git push` returning success has no fire-and-forget failure mode, so this is a STRONGER landed-guarantee than the Drive listing check it replaces, not a weaker stand-in. **Scope note: the AQE feed (premarket step 3) is UNCHANGED** — that is an external engine's own Drive publish channel, a different system boundary the kernel does not control; this decision only removes the kernel's OWN duplicate write of a file it already had in git. **(2) Pipeline Ledger filing moves into premarket.** The committee already decides `ledger_proposal` live during premarket's deliberation (step 9); `tools/pipeline_ledger.py persist` now runs immediately after `committee.json` is written, same session — not held until the next post-market run. This is why: a `trigger_silent` name needs to be visible on List P (D-83) for the SAME DAY's market-hours sweep, and List P is built at premarket step 10 right after persist now runs at step 9 — filing at post-market would have made every same-day proposal invisible to intraday monitoring until the FOLLOWING day. Post-market keeps only `sweep` (end-of-day TTL expiry + trigger evaluation against today's close), which genuinely is an end-of-day job. **(3) Post-market restructured to match (D-84, PM's item-by-item ruling on the walked procedure):** reordered to journal → portfolio check → archive → ledger sweep → audit, immediately after the journal (previously portfolio check ran after two Drive round-trips). Removed the nightly trailing-stop recompute (post-market step 2c) — it ran BEFORE AQE's export existed each morning, so it was computing off yesterday's structure and could push a TIGHTEN_STOP request off stale data; premarket step 4 already does this correctly, right after its own fresh AQE pull, so this was duplicate work with a real risk, not just redundant compute. Removed the day-performance scorer (post-market step 2b, `RB:performance`) — verified by direct inspection that `RB:performance` does not exist anywhere in `charter/rulebook.yaml` and no tool ever implemented the scorer this step described; `tools/voice_memory.py update` already tolerates a missing scorecard (`--scorecard` defaults to `None`) and derives its real stats from the Nomination Ledger, so nothing downstream broke. The phase-gate stamp (D-83) is folded into the audit step as its last action instead of a separate numbered step. The final run-outcome step is now a flat mechanical pass/fail checklist per job, not a narrative headline. | The Drive PTJ/archive write path and D-69's Drive-verification apparatus (`tools/drive_ptj_check.py`, `ptj_drive_check.json`); post-market as the Pipeline Ledger's filing point (post-market keeps only the end-of-day sweep); post-market's nightly trailing-stop recompute (premarket step 4 is now the only one); post-market's day-performance scorer step (cited a rulebook key — `RB:performance` — that was never defined and a tool that was never built) |
| D-83 | 2026-07-28 | **Post-market becomes the PRE-REQUISITE GATE for premarket's expensive half, and a PIPELINE LEDGER holds the ideas the committee itself asks to keep alive (PM: "half 1 should be ready by a certain time in the morning, the pre-requisite is post market journal updates… as for the ledger, we dont add everything, we only add what committee proposes to be in the ledger based on quality of setup… we can consider if Phase 0 is actually a scheduled task which i believe is lighter on token use").** Three parts. **(1) The gate.** New `tools/phase_gate.py` (deterministic, law 4): post-market's LAST write is `stamp --phase post_market --status ok|fail|partial --journal-date <date>`; `check` verifies the stamp is today's, that the journal it claims **actually exists and parses on disk** (D-69 — audit the world, not the kernel's belief about the world), and that `output/aqe_daily_export.json` is dated today and non-empty. Three exit codes: 0 READY, 1 NOT_READY (transient — retry, page only past RB:schedule.phase_zero.page_after_sgt), 2 BLOCKED (post-market failed or its journal is missing — page now, time will not fix it). **(2) Phase 0 as its own cheap scheduled task.** New `skills/phase_zero/SKILL.md` — two file reads, no models, no swarm — runs `check` on a repeating schedule (RB:schedule.phase_zero) and fires the premarket build only on READY. Because a scheduled Cowork task starts a FRESH session with no memory, repetition is made safe by an on-disk exactly-once latch (`phase_gate.py claim`), released on a fire failure so the morning can still retry. This replaces the clock guess (premarket scheduled a few hours after post-market) and moves the freshness check OUT of the 12-spawn session that used to pay to spin up before halting. **(3) The Pipeline Ledger.** New `contracts/pipeline_ledger.schema.json` + `tools/pipeline_ledger.py` + `data/persistent/pipeline_ledger.json` — a LIFECYCLE store for ideas that have NOT fired yet, distinct from the existing Nomination Ledger (an OUTCOME store scoring d1..d15 on names already nominated). The committee decides membership: `contracts/committee.schema.json` gains an optional `ledger_proposal {propose, classification, trigger, reason}` per verdict, and committee-desk step 8 makes that call ONCE, live, at deliberation time — no extra spawn, no second judgement pass. Two modes: `daily_reconsider` (re-fed into tomorrow's tally, re-scored fresh) and `trigger_silent` (parked silent until a `{field, op, value}` condition fires on the market-hours sweep). Post-market step 2d is FILING ONLY (law 4, no model): it persists exactly what the committee proposed and nothing else — **no blanket sweep of everything that failed to advance** — and rejects two contradictions loudly (a `trigger_silent` with no trigger; a proposal on an ADVANCE name, which belongs in the plan, not the pipeline). A missing field NEVER satisfies a trigger. TTL 30d / 60 rows (RB:pipeline_ledger). **Both classifications have a live consumer — the ledger is not a write-only store.** `daily_reconsider` rows re-enter premarket step 6 as CARRY-OVER entries in the deliberation set, carrying their `case_snapshot` and `origin_date` as context and re-scored fresh from today's universe (the snapshot is never a score and never a shortcut past the threshold). `trigger_silent` rows join the daily alert longlist as **List P** (`tools/alert_list.py`, D-82's A∪B∪C becomes A∪B∪C∪P) carrying their `{field, op, value}` trigger and case, so the market-hours 30-min sweep sees the stated condition fire in real time instead of a full session late at post-market's end-of-day sweep — a parked name is by definition not in the plan and usually not strong enough for List A or B, so without List P it would be invisible intraday. Lifecycle closes itself: an ADVANCED name's row is closed automatically at post-market persist, so one idea is never both live in the plan and 'watched' in the ledger. Surfaced in the EXISTING 10:00 morning summary — active counts plus any name whose trigger fired, with its case snapshot — not on a new surface. Premarket step 1 now defers to the gate instead of re-deriving freshness. Also fixed audit item F2: `schedule.alert_feed_minutes` and `schedule.market_watch_sweep_minutes` 15→30, missed when D-82 moved the feed. | The clock-guess dependency between post-market and premarket; premarket step 1's duplicate hand-rolled freshness check; the implied "post-market classifies/sweeps the leftovers" ledger model (the committee proposes, post-market only files); the assumption that the expensive premarket session is the cheapest place to discover its own inputs are stale |
| D-82 | 2026-07-22 | **The daily AQE alert list is an AMALGAM of three reused lists — A∪B∪C — and the intraday alert is a BRACKET-LEVEL event on that longlist, 30-min file feed not 15-min email (PM: "first thing is a Universe (widest net) → AQE Scoreboard → fork 3 lists: (A) top 50 by tiering Data+Detect, (B) strongest 5D Elder top 15, (C) up to 10 ideas per committee voice stored in a dated premarket-review json; amalgamate A+B+C into the daily AQE alert list… intraday flag by bracket levels (Entry/SL/breakout +2% over last COB), alerts carry category+why not full PTRS, change 15min→30min, market-hours agent picks up the file and requests committee deliberation. Reuse and consolidate, don't spawn new lists").** Refines D-81's alert-universe (which had made the casting mat the sole definition): the alert list is now **List A** (`tools/alert_universe.py` casting-mat, top-50, data+detect — REUSED) ∪ **List B** (strongest 5-day Elder top-15 — the one genuinely new selection, thin: ranks `elder_5d`, falls back to the `elder` composite until AQE populates elder_5d) ∪ **List C** (the committee voice nominations, up to 10/voice — REUSED read-only from `data/sod/DATE/nominations/*.json`, also persisted as the dated `premarket_review.json`). New `tools/alert_list.py` (deterministic, law 4, selftest-verified) amalgamates by ticker, each entry tagged with its categories (A/B/C) + why + bracket levels (entry, +2%-COB breakout, stop, targets); validated live 2026-07-21: A50 ∪ B15 ∪ C63 → 85 unique, 39 multi-category. **Committee feed UNTOUCHED** — List C only reads existing nominations; universe/tally/deliberation/committee flow unchanged (PM: "make sure it's still completely intact what we feed to committee"). Intraday: `contracts/alert_inbox.schema.json` extended (not replaced) with `category` + bracket-level `alert_type`s (`entry_cross`, `breakout_2pct_cob`, `sl_approach`, `sl_hit`); cadence 15→30 min and email→file (Drive "AQE" folder) in `parameters.yaml→alert_list` + market_hours step 1; the market-hours agent's one job = request committee deliberation on an on-list opportunity event. `docs/AQE_SLIM_EMIT_SPEC.md` extended: AQE must populate `elder_5d` and emit the 30-min alert file (not email) on the alert-list membership. premarket step 10 builds the list after committee; step 11 loads it. | D-81's casting-mat-as-sole-alert-universe (now List A of three); the 15-min email alert feed; the framing that voices are excluded from the alert universe (they return as List C, one of three inputs — distinct from the retired standalone "voice top-10s" hand-pick) |
| D-81 | 2026-07-22 | **Premarket universe→AQE→lens→alert-universe pipeline CONSOLIDATED — one source, one data+detect view, one landing, trimmed JSON (PM: "make sure premarket builds the correct universe, runs AQE on it, applies the right lens to be today's live AQE alert universe, and we know where it lands and what agents do about it… it's patched all over the place… clean up the AQE output JSON, too many = noise").** A 3-lane audit found the pipeline worked by accident, not design: (1) **universe build** ran a redundant kernel-side FMP screen (`universe_screen.py`) that is dead in the scheduled container (no FMP_API_KEY), so the live universe was hand-assembled from the AQE export → 3 incompatible `universe.json` shapes in 3 days, near_misses lost, contract too loose to catch it; (2) the **data+detect logic was duplicated** in two files (`alert_universe.lanes_for` vs `conviction_funnel._lane_count`), neither reading `parameters.yaml`; (3) **three competing definitions of "the alert universe"** (the D-77 casting mat — built but ORPHANED, wired to nothing; premarket step-11's pre-D-77 "voice top-10s + held book" hand-pick; and AQE's `inbox.jsonl` at a different floor 65); (4) the **AQE export = 97 fields/name + ~42 KB/day of self-describing blocks no code reads**, byte-duplicated into `universe.json`, re-copied into 11 per-voice files, with `subcomponents` shipping empty `{}` though voice cards read it. FIX (PM-confirmed: FMP feeds AQE upstream so the kernel must not re-screen FMP; "build the same view as the daily deliberation list — data+detect"; trim both sides): **new `tools/lanes.py`** = the single 8-lane definition + `load_params()` reading the charter (both `alert_universe.py` and `conviction_funnel.py` now import it — 0 lane-count mismatches across 142 live records, proving behaviour-identical); **new `tools/universe_build.py`** shapes one schema-enforced `universe.json` from the AQE export, each name trimmed 97→47 consumed fields (incl. Thorp's `sc_m_gate_detail`/`sc_p_gate_detail`), near_misses restored; `universe_screen.py` retired to a standalone FMP tool (kept, deprecated); `contracts/universe.schema.json` tightened to reject drifted shapes; `contracts/aqe_export.schema.json` self-describing blocks made non-required; **`alert_universe.py` WIRED** at premarket step 10 → the one canonical `data/alerts/DATE/alert_universe.json`, loaded on approval at step 11 (the "voice top-10s" language retired); market-hours + `alert_inbox.py` floor aligned to 70; **`docs/AQE_SLIM_EMIT_SPEC.md`** written for the external AQE engineer (stop emitting the 5 bloat blocks, prune ~30 never-consumed fields, POPULATE `subcomponents`, reconcile field names). Deliberation list and alert universe now provably share one lane engine — they can never diverge. All tool selftests pass; end-to-end validated on 2026-07-21 SOD data. **Follow-up (staged, not silently dropped):** `alert_inbox.py` still re-derives strong-universe membership by floor rather than reading `alert_universe.json` directly — bounded intraday change, task open. | `universe_screen.py` as the production universe source; the duplicated lane logic in two files; the ignored `parameters.yaml` for both tools; the orphaned/hand-named `alert_universe_castingmat.json`; premarket step-11's "all voice top-10s + held book" alert-universe definition; the 3-shape `universe.json` drift; the 97-field export byte-copy + 11 per-voice universe files as the shipped shape |
| D-80 | 2026-07-21 | **LOCKED — the canonical Aegis selection doctrine (PM: "data leads, lens seconds, voices corroborates or challenges; ultimately forms consensus for high conviction. Let me know once locked").** The single authoritative statement of how the day's high-conviction set is formed, now embedded in `tools/conviction_funnel.py` (header), `parameters.yaml→conviction_funnel.doctrine_locked`, and premarket steps 6b/9: **DATA leads · LENS seconds · VOICES corroborate or challenge → consensus for HIGH CONVICTION.** Precisely: DATA (the D-77 8-lane casting mat = momentum arriving) is primary; LENS (the 6-lens detect) is the second confirmer; data OR lens strong sets the ~10–15 mechanically-confirmed shortlist; the VOICES then CORROBORATE (nominate/add conviction), stay SILENT (advisory), or CHALLENGE (dissent/bear case, or vote a name the mechanicals don't confirm); **HIGH CONVICTION = mechanical confirmation AND voice corroboration, synthesised by the committee.** A mechanically-strong name the voices challenge, or a voted name the mechanicals don't confirm, is a deliberation item the committee must resolve (run or drop) — never an auto-advance. Voices corroborate or challenge; they never buy a seat alone. This locks and completes D-78 (record the lane methodology) and D-79 (data+lens first); no code change beyond doctrine embedding — the funnel already implements it. | Any residual reading of the three axes as co-equal, or of a voice vote as sufficient on its own to confer high conviction; the doctrine is now fixed and cited, not re-litigated per run |
| D-79 | 2026-07-21 | **Conviction-funnel axis ORDER fixed: DATA + LENS are primary/mechanical, CONSENSUS (voices) is an ADVISORY overlay — refines D-78 the same day (PM: "I think it's data but lens first before we anchor on voices advisory").** D-78 graded the three axes symmetrically (a name entered the shortlist on ≥2 of 3 strong, so a high vote count alone could carry it). Corrected: the PRIMARY membership test is now MECHANICAL — a name enters the ~10–15 shortlist iff **DATA (lanes) strong OR LENS (detect) strong**, ranked by lanes+detect first with votes only a tiebreak; the voices then OVERLAY as CONFIRMED (swarm also backed it) or advisory-silent (data/lens say go, swarm under-nominated — promoted into the shortlist anyway). Names VOTED well but not mechanically confirmed drop OUT of the shortlist into the `consensus_only` contradiction list for explicit run/drop rulings. Material effect on 21 Jul: DINO **and the top-headline-momentum UNP (sc 80) and CSX (4 votes each)** leave the shortlist — their lane depth is only medium and lens weak, so under a data-first rule they are "voted but unconfirmed," not core; meanwhile data-strong-but-voice-light names (PSX, RSI, WT, HR, and the detect REIT cluster) are PROMOTED into the shortlist as DATA_LED. `tools/conviction_funnel.py` rewritten (selftest updated + passing); `parameters.yaml→conviction_funnel.axes` reordered to [data, lens, consensus] with `primary_axes: [data, lens]`; premarket step 6b reworded. This is the momentum-book-correct emphasis — mechanical momentum-arrival first, human consensus as corroboration, not the anchor. | D-78's symmetric ≥2-of-3-axes shortlist entry; any path where a high vote count alone puts a mechanically-unconfirmed name into the high-conviction shortlist |
| D-78 | 2026-07-21 | **The lane methodology is now recorded into the DAILY PREMARKET run as a three-axis CONVICTION FUNNEL (PM: "record this lane methodology into our daily premarket runs. This is what I want; data driven, lens and consensus driven. Concise summary highlighting contradictory or positive overlaps that we can deepen deliberation. Always start from approx 10–15 names to filter down into a high conviction plan").** New `tools/conviction_funnel.py` (deterministic, law 4, selftest-verified) runs at premarket step 6b, after the tally/event filter and before deliberation. It grades every nominated name S/M/W on THREE independent axes — CONSENSUS (voices: nomination count + avg conviction), DATA (the D-77 8-lane casting mat = momentum-arrival depth), LENS (the 6-lens detect count) — and emits: (1) a CONVERGENCE SHORTLIST of ~10–15 names strong on ≥2 axes (the high-conviction start-point for deep deliberation, replacing 'deliberate the whole ~63-name nomination union' as the working set); (2) CONTRADICTIONS TO RESOLVE — `consensus_only` (voted but data+lens don't confirm → fresh or stale? e.g. DINO 4 votes/2 lanes/1 lens) and `data_lens_only` (≤1 vote but mechanically/lens strong → did the swarm miss a runner? e.g. the detect-strong REIT/insurance cluster HR/BNL/SPG/MTG/ALL/CURB, and PSX/RSI/DJT). Thresholds in `parameters.yaml→conviction_funnel` (PM-tunable). It is TRANSPARENCY/decision-support, NEVER a gate (D-4/D-52) — it focuses and frames deliberation, never removes/advances a name itself; the committee deliberates the shortlist AND must explicitly RESOLVE each contradiction (run or drop, with reason) — an unaddressed contradiction is an incomplete deliberation. Validated 21 Jul: 62 nominated → 14-name shortlist (triple-convergent SHO; consensus+data overlaps FBP/CTVA/EFX/NOMD/NHI/NWBI/DRH; data+lens-convergent-but-voice-light REIT cluster) + 3 consensus-only + 6 data/lens-only contradictions. Complements (does not retire) the D-60 data board. | 'deliberate the full nomination union with no convergence pre-focus'; the earlier implicit treatment of a high consensus vote as sufficient on its own (DINO); any assumption the detect lens or the vote alone identifies the day's high-conviction set |
| D-77 | 2026-07-21 | **The AQE alert universe is a MECHANICAL "casting mat" — the wired implementation of the D-57 flag_condition doctrine, with the 2-day lane enrichment (PM: "screen through four or five key subcomponent scores and also use a certain detect lens… so when AQE alerts bring the idea to the committee we can access these numbers to decide if it's a runner"; "limiting it to our singular view of names seems overly constrictive").** Resolves a 2-day tension on how stocks are longlisted into the intraday alert feed. Rejected: the old raw `sc_momentum>70` (57 names, noisy), a tightened `sc_momentum>80` (2 names, too narrow — good-but-not-top sc still runs with structure/elder around it), and the committee hand-picking ~37 names (too rigid, misses genuine runners). KEY empirical finding, from the PM's own examples (BILL, ETSY ran last night): the 6-lens detect count ALONE misses real runners — BILL scored detect 0/6 yet carried `sc_m_gates` (all 5 momentum sub-gates), a BULLISH change-of-character, and a significant KNN. So detection must be a COUNT of 8 momentum-incoming LANES (5gates, choch_bullish, knn_significant, detect_lens≥4, rs_leadership, structure≥72, flow≥68, mp_accel≠decelerating), detect as one OR-booster never a gate. New `tools/alert_universe.py` (deterministic, law 4, selftest-verified): membership = `sc_momentum≥70 AND lanes≥2`, TIERED by lane depth (T1≥5 priority / T2 3-4 / T3=2 watch), event-driven names struck (PYPL M&A). Thresholds in `parameters.yaml→alert_universe` (PM tunes without code). Every fired alert carries `lane_count+lanes_fired` into the market-hours pod (D-63) — the SAME numbers the voices read, so one evaluation logic at SOD and intraday. Validated 21 Jul: BILL(6 lanes)+ETSY(5)→Tier1; DINO(2 lanes, 25% ext)→Tier3 (correctly demoted from the earlier "#1 momentum" call — its high headline score is a stale extended move); 56 names (40/10/6). Clean separation ratified: the UNIVERSE is a mechanical formula (wide, tunable, no hand-pick); the COMMITTEE/pod applies judgment only to what FIRES — the committee does NOT pre-select the universe. Also this session: full handoff pack authored (`handoff/00..04`, `PLATFORM_SWITCH.md`) consolidating requirement/build/agentic-design/orchestration/runtime-infra-sourcing-spec. | The raw `sc_momentum>70` alert feed; the committee hand-picked alert list (21 Jul); the earlier framing of DINO as a top momentum pick on headline score alone; any reading that the detect-lens count alone identifies runners |
| D-76 | 2026-07-21 | **The RESULTS-DELIVERY cockpit — kernel-native, so autonomous output appears where the PM is actually looking, and it survives a session ending (PM: "U need to pack this into the kernel, so when this session ends or gets full it's repeatable in the next session... kinda stupid agents do their work with no visible output").** Root cause of a week of "agents work, I see nothing," finally correctly diagnosed: scheduled triggers fire a BRAND-NEW ephemeral session the PM "views independently," and on MOBILE those scheduled-task sessions are not surfaced yet + the push did not reliably arrive — so a loop could complete perfectly (git proves it) and the PM saw nothing. D-75's "end with a chat message" fix could not solve this because a fresh session cannot post into the chat the PM is reading. The ONLY mechanism that delivers into the PM's live session is `send_later` (a self-bound Routine → this session) — PROVEN live this session (the check-in prompts and an autonomous ops-card render both landed in-session on cue). New `skills/cockpit/SKILL.md` (+ `/cockpit arm` / `/cockpit status`, commands.md) packages it: compute stays on the durable cron triggers (fresh bootstrapped sessions, push to git, D-64 — no work ever lost), and a SEPARATE lightweight render loop pulls the freshest git state and displays it in whatever chat has run `/cockpit arm`, via `send_later` deliveries timed just after each cron pushes (post-market 05:25 / D&R 08:20 / premarket 10:40 / market-hours wrap 04:10 SGT), each delivery re-arming tomorrow's so one arm self-perpetuates. Reuses `ops_status.py`/`/status`/`morning_summary.py`/`daily_flow_audit.py` for rendering — no new render code (anti-spaghetti). **STATED LIMITATION, not buried:** a `send_later` survives its session compacting/"getting full" (same session id) but NOT the session being truly closed/replaced by a new chat — there is no API to inject into a session that does not yet exist. So the durable contract is: within a session, fully autonomous once armed; across a genuinely new chat, run `/cockpit arm` ONCE to re-establish (and because compute+git are durable, the new cockpit shows correct current state instantly — nothing lost in the gap). CONTEXT.md updated so a fresh session self-advertises "cockpit not armed — run /cockpit arm." | D-75's framing that the notification path alone could make scheduled output visible; the CONTEXT.md assumption that phone pushes + independently-viewed scheduled sessions were a sufficient interaction surface for the PM |
| D-75 | 2026-07-21 | **Root-caused a silent-but-successful run: today's premarket completed cleanly (10:00 SGT fire, full 11-voice swarm, committee deliberation, plan pushed to git ~03:11 UTC) but NO phone notification arrived — PM: "its 4:24pm? dont see anything...".** Direct inspection confirmed the run itself was NOT the problem: `data/sod/2026-07-21/` has all 11 nomination files, `committee.json`, `plan.json` (real content — YELLOW/MEAN-REVERT posture, IBM/NTAP exits, PTRN partial, 12 ADVANCE previews), committed by the bootstrap's own git identity at 03:11:45 UTC (11:11 SGT), well inside the 16:00 SGT plan-ready target. The break is in `tools/notify.py`'s `cowork` channel: its code comment claimed "the printed message IS the Cowork session's output; the push carries it" — but a scheduled task's push notification is actually built from the session's FINAL ASSISTANT CHAT MESSAGE, not from a tool call's stdout. Every loop's notify step (`tools/notify.py run_ok/run_fail`) was a bare tool invocation with nothing downstream ensuring the orchestrator then SPOKE that body as its closing reply — so a run could complete perfectly and still page nobody. Fixed: `notify.py`'s cowork-channel comment + printed line now say plainly that the print is a draft, not delivery; `premarket` step 12 and `post_market` step 5 both gained an explicit closing instruction — after calling `notify.py`, the session's LAST action must be a plain-language chat message carrying that same run_ok/run_fail body. Not yet independently verified against a live fire (next real test is tonight's market-hours liveness 21:25 SGT and tomorrow's post-market 05:05 SGT) — flagged, not asserted fixed until confirmed. Today's actual plan was hand-surfaced to the PM directly in-session since the approval clock (21:00 SGT) doesn't wait for the next scheduled fire. | The `notify.py` cowork-channel assumption that a tool-call print is equivalent to phone delivery; any prior claim that a "silent" run necessarily means the run itself failed |
| D-74 | 2026-07-21 | **Three scheduler fixes from a live trigger audit (PM: "post market should be 5:05am so we dont have to change it because of any daylight saving time change... I dont see any market hours Loop... where do I delete the old ptj?").** (1) **Post-market moved 04:05→05:05 SGT, DST-proofed.** 04:05 SGT was calibrated to US close under EDT only; under EST (UTC-5, Nov–Mar) the same close lands at 05:00 SGT, meaning the old slot would have fired ~1hr BEFORE the market actually finished closing for roughly half the year — a real staleness bug, not a hypothetical. 05:05 SGT is safe in both DST states permanently; old trigger (`...hNfPQ5`) deleted, new `trig_01FWqohNLAnML72pnYCgvdc6` created (cron `5 21 * * 1-5`). (2) **The intraday market-hours loop was never actually armed — a real gap, not a misunderstanding.** D-63/D-70 both assumed a `/loop` was live sweeping the 21:30–04:00 SGT window; a direct audit of every live trigger this session found none — the old hourly market-watch cron was deleted when the `/loop` pattern was adopted, but no trigger was ever created to HOST that loop each evening, so the intraday sweep had been silently absent since market-watch v2's deletion. Fixed: new `trig_01MjvFDC4Yd94cdk6U5rAu7S` ("market-hours liveness v1") fires once at 21:25 SGT wkdays, confirms liveness, sweeps the alert inbox, then self-rearms via `ScheduleWakeup` every 30 min until 04:00 SGT close, then stops — closing the gap for tonight's session (first fire 21:25 SGT, 21 Jul). (3) **Old PTJ triggers deleted — clarified these are scheduled triggers, NOT the disabled `print-trade-journal` skill (D-67).** Two separate objects share the "old PTJ" label: the standalone Cowork skill `print-trade-journal` (already disabled, harmless, superseded by D-67's kernel-native emission, no action needed — a disabled skill runs nothing, nothing to clean up) and three orphaned scheduled triggers from the pre-D-67 era ("PTJ → Archive → Dashboard," "Trade Journal Daily Archive + PTJ Staleness Check," "Trade Journal Daily Archive Append," created 07-09/07-11/07-16, all dead/`enabled:null`, superseded by post-market v2's kernel-native PTJ+archive) — these ARE deletable via `delete_trigger` and were deleted this session. Also deleted: three inert `run_once_fired` one-shots surfaced by the same audit. | D-64/D-70's post-market 04:05 SGT slot; the D-63/D-70 assumption that an intraday `/loop` was already armed; the three superseded PTJ-chain triggers and the old post-market v2 trigger |
| D-73 | 2026-07-21 | **Voice-agent spawning STAYS on the plugin `subagent_type` mechanism — git-read migration NOT pursued (PM: "Ok so we stick to plugins? Where the non analysis non self heal agents are all cheap cost").** This session had offered migrating voice-agent spawning (10 voices + committee-desk, `agents/voice-*.md`) off the compiled-plugin mechanism onto the same git-clone-and-read pattern already used for skills/tools (D-64), to close the one remaining staleness risk (a rebuilt agent not reaching a scheduled session until the plugin is reinstalled). PM declined for now: the existing discipline of rebuilding + reinstalling the plugin on every agent change already covers that risk operationally, and taking on the migration's engineering cost doesn't address the separate, more pressing concern raised the same session — Claude Max 5x-plan usage exposure (D-72) — since spawn mechanism (plugin vs git) is orthogonal to model-tier cost; the judgment-tier swarm is exactly as expensive either way. Cost split reconfirmed: `tools/*.py` = zero model cost (deterministic code); market-watch + the control-plane orchestrator = cheap tier (haiku / economy, D-58/D-16 by design); the 11 opus-tier premarket spawns remain the actual cost driver, unaffected by this decision. | This session's own offer to migrate voice-agent spawning to git-read; any assumption that the plugin mechanism is being phased out |
| D-72 | 2026-07-21 | **New `usage_limit` failure class in `tools/self_heal.py` (capacity, D-45's exception ladder extended, not duplicated) — detects a Claude Max-plan session/rate/weekly-cap signature on a subagent spawn failure and escalates distinctly from a voice simply nominating badly (PM: confirmed the 5x tier / 150 SGD plan and asked to "build the check for future reference before we do any migration").** Context: Anthropic pools Claude Code/Agent SDK/scheduled-task usage against the SAME 5-hour + weekly caps as ordinary chat, with no published automation-specific allowance and no documented behavior for a scheduled session hitting the cap mid-run — a real, not hypothetical, risk given premarket's 11 opus-tier spawns (10 voices + committee-desk) daily on a 5x plan. New deterministic classifier `detect_usage_limit()` (law 4, selftest-verified) pattern-matches a spawn failure's error text against known limit signatures ("rate limit", "usage cap", "resets at", "429", etc.); a match routes to the new `capacity` class (no auto-retry — retrying now just re-hits the same ceiling — immediate escalate with "wait for the reset, then rerun `<loop>`"), reusing the SAME `heal()`/`MANUAL_FIX`/logging/notify path as every other failure class rather than a parallel system. Wired into `premarket` step 5 (the swarm) and step 9 (committee-desk) so a cap-hit spawn failure is never mislabeled INCOMPLETE (the existing "voice returned invalid" path) or "deliberation cannot complete." **Stated limitation, not silently assumed away: this can only catch a failure the ORCHESTRATOR session survives to observe — if the orchestrator's own session hits the cap, nothing runs to catch it.** That residual risk is why the PM is separately evaluating metered API billing for the scheduled pipeline instead of the consumer Max plan; this tool is a stopgap for visibility, not a full fix, and no migration was undertaken this session (explicitly deferred per the PM's instruction). | Any prior silent gap where a mid-swarm usage-cap failure would have looked identical to a generic INCOMPLETE run or a data-pull failure |
| D-71 | 2026-07-21 | **Aegis hedge scope confirmed as Tiger-only (XLK/XLF); IWM/QQQ/TLT and every single-name option on both brokers are Protégé9, not Aegis (PM, after a full line-by-line pull of both brokers' option books: "only the tiger set is AEGIS related. Everything else is not — it's part of protege9 portfolio").** A prior read of this session (in response to the PM's "isn't IWM/QQQ hedge spreads also included?") wrongly treated IBKR's IWM/QQQ/TLT put spreads as UNRECONCILED Aegis hedges missing from the journal, and was preparing to propose wiring them into `journal.hedge{}` kernel-natively (D-67/D-68 pattern). PM correction (confirmed scoped to hedges/options only — the 12-position core stock book, 4 Tiger + 8 IBKR, is unaffected and unchanged): those three, plus the IBM options overlay (IBKR 300C/330C + Tiger 260C), plus every other single-name option on both brokers (AEHR/CTOS/DUOL/ICHR/MRVL/TGB on IBKR; AMAT/COIN/CRCL/ECHO/GLW/INTC/MARA/MSFT/NFLX/NVDA on Tiger) belong to the Protégé9 book. **Net effect: the journal's existing `hedge{}` block (XLK/XLF only) was already correct all along — no kernel gap, no fix needed; the D-67/D-68-style reconciliation this session was about to propose for IWM/QQQ/TLT is explicitly NOT built (would have been over-engineering a non-problem, PM's standing anti-spaghetti directive).** Logged so a future Design & Review pass doesn't re-flag XLK/XLF-only as an incomplete hedge picture. | This session's own draft proposal to wire IWM/QQQ/TLT into the Aegis journal hedge block; any prior framing of those three as "unreconciled Aegis hedges" |
| D-70 | 2026-07-21 | **AQE feed pull gets bounded retry BEFORE stand-down, and the resulting page states the manual fix plainly (PM: "review the timing windows… what happens if AQE errors out? Who catches it and how is a rerun scheduled? I'm ok to not automate this but minimum is I am notified to resolve manually — trigger a fixed run").** Timing review: AQE's observed pipeline (its own `aqe_last_run.json`) starts 08:30:54 SGT, exports 08:43:03, finishes 08:44:29 — a ~14-min job — against premarket's 10:00 SGT fire, a ~75-min buffer (5x its own runtime); Design & Review (08:00) and post-market (04:05) don't depend on today's export at all, so no chronology conflict exists on a normal day. The gap was resilience to an ABNORMAL day: D-66's step 3 short-circuited straight to stand-down on the first stale/missing check, bypassing the bounded-retry doctrine already established everywhere else in the system (`tools/self_heal.py`, `feed_pull` = transient class) — corrected here. Premarket now retries the Drive pull at +5/+10/+10 min (~25 min worst case, self-heals an AQE run that's simply a few minutes late, silently, no page) before declaring a real failure. Only after 3 exhausted retries does it stand down and page (`tools/notify.py run_fail`) — and the page states the exact manual lever (`self_heal.py`'s `MANUAL_FIX["feed_pull"]`, `recover/SKILL.md`'s `/repull` entry, both updated): confirm AQE has actually finished, then tell Claude "rerun premarket" — that re-fires the trigger on demand via `fire_trigger`. The kernel never controls or auto-retriggers AQE itself (no automation of the AQE-side fix, per the PM's explicit scope). | D-66's step-3 immediate stand-down (no retry) as the AQE-failure response |
| D-69 | 2026-07-21 | **Independent Drive-PTJ freshness check closes the audit blind spot the PM identified (PM: "why wasn't this captured? ... is [Design & Review] doing its job?").** Root cause, confirmed by direct inspection: every existing audit tool (`daily_flow_audit.py`, the auditor, Design & Review) reads ONLY local repo/workspace files — none of them ever called Drive, so a scheduled job living entirely outside the kernel (the old `print-trade-journal` skill) was structurally invisible to them; the auditor wasn't negligent, it never had eyes on that job. New `tools/drive_ptj_check.py` (deterministic, law 4, selftest-verified against both a real listing and synthetic edge cases) takes a live Drive search result + target date and returns FRESH/STALE/MISSING — an INDEPENDENT check of what Drive actually shows, not what the kernel's own write step believed it did (catches a silent partial failure: kernel thinks it wrote, Drive write actually errored). Wired as `post_market` step 3, right after the archive step: search the PTJ folder, run the check, write `data/eod/DATE/ptj_drive_check.json`; `daily_flow_audit.py`'s `operations_desk` layer now folds this in (`ptj_drive_status`/`ptj_drive_verified`) and a non-FRESH verdict lands in the exceptions list, so it surfaces in the flight recorder itself, not just as a one-off page. Verified end-to-end against real Drive data (07-20 FRESH, 07-21 correctly STALE pre-run). | The audit blind spot where a Drive-only write path was invisible to every kernel assurance tool |
| D-68 | 2026-07-21 | **BL-046 closed — the archive-ledger append + housekeeping check are now kernel-native too (PM: "this is a key function to build the archive… once you build it I'll shut down the existing daily co work").** Completes D-67 (PTJ emission). New `tools/archive_ledger.py merge` (deterministic, law 4, selftest-verified against the REAL 07-17 Drive archive schema): merges today's newly closed trades into `closed_trades_ledger` (Golden Rule dedupe by ticker+exitDate, newer wins — idempotent on re-run, proven by selftest), recomputes YTD/QTD/MTD + by-sector + by-trading-day, and INTEGRITY-GATES the write (raises if `sum(by_trading_day) != YTD`, never ships a broken archive). Wired as `post_market` step 1b: no new trades → no-op, no write (retired skill's rule, kept, verified byte-identical output on the no-op case); new trades → write back to Drive, same title, same PTJ folder. Housekeeping (>10 PTJ files → flag) ships too — **as a flag only, not an auto-move**, because the Drive connector genuinely has no delete/move tool (same limitation the retired skill had; PM cleans up manually in the Drive UI, unchanged). **PM ACTION CONFIRMED: once this ships, disable the old "Daily Routine: PTJ → Archive → Dashboard" trigger (already disabled 21 Jul) to prevent a cross-session duplicate write** — the kernel is now the sole writer of both the PTJ and the archive. | The BL-046 gap left open by D-67; any remaining reliance on the standalone `print-trade-journal` skill for archive/housekeeping |
| D-67 | 2026-07-21 | **PTJ generation moved INTO the kernel — one installer set, not a scattered standing skill (PM: "include it in the plugin so it's a whole installer set, not all over the place").** Root cause of the stale-PTJ finding (last file 07-17): the "Daily Routine: PTJ → Archive → Dashboard" scheduled task called a standalone user skill (`print-trade-journal`) that was silently `enabled:false` — almost certainly switched off when the aegis-v4 plugin was installed/updated, since it lives outside the plugin and nothing in the kernel keeps it in sync. Rather than re-enabling a skill a future reinstall can silently kill again, `post_market` step 1 now emits the PTJ directly: the same validated `aegis_journal_DATE.json` record is ALSO written to Drive as `aegis_trade_journal_[DATE]_PTJ.json` in the canonical PTJ folder (`mcp__Google_Drive__create_file`, locked naming convention preserved for any consumer still searching for it) — no external skill, no toggle to forget, ships with every plugin build. Verified live: `aegis_trade_journal_2026-07-20_PTJ.json` written from the already-proven 07-20 journal (commit d67996e's data) to close the immediate gap. **Scope note (BL-046, captured not dropped):** the retired skill also did two more things — appended closed trades into a running `ARCHIVE_master.json` with YTD/QTD/MTD rollups, and a >10-file housekeeping/move-to-Legacy check. Those are NOT yet replicated in-kernel; captured as BL-046 for a future post-market extension rather than silently lost. | The standalone `print-trade-journal` skill as the PTJ writer of record (still exists, now unused by the kernel — the PM may re-enable it manually if wanted, but the kernel no longer depends on it) |
| D-66 | 2026-07-21 | **The AQE daily export is PULLED from the Drive "AQE" folder — not waited for on disk (PM: "there's only really one google drive folder that's the feed").** Design & Review blamed the empty Ledger on "no AQE export reached the workspace" and proposed a PC-side git push (over-engineered). Reality: `aqe_daily_export.json` is ALREADY in the Drive folder named "AQE" (id 1CJMoI19Zf_ZFeU5_5uhW9l92IB8fVger), refreshed daily (07-21 copy 00:44Z, 1.18MB), and a fresh scheduled session already carries the Google Drive connector. So premarket step 3 now PULLS the export straight from that folder (download → output/aqe_daily_export.json → freshness check → schema → tripwires); a stale/missing export stands the phase down with a page rather than proceeding on a thin universe. The premarket trigger prompt carries the same pull as an explicit step so it works before the next reinstall. The intraday `aqe_ma_scan.json` alerts feed should live in the SAME "AQE" folder (one feed location). | The review's BL-044 "PM wires a PC/HF git push" framing; the implicit assumption the export would be pre-staged on the workspace disk |
| D-65 | 2026-07-21 | **The swarm ALWAYS runs — a portfolio breach caps verdicts, never analysis (BL-043, PM-approved).** Design & Review found the voices ran 0/11 for three straight sessions (18–20 Jul): a portfolio hard-gate breach (sector concentration) was taken as a reason to skip the premarket swarm. That is a process deviation — constitution §4 invokes all voices ALWAYS; a hard breach blocks NEW ENTRY (RB:gate_doctrine), not thinking. Fix: premarket step 5 now states explicitly that a breach constrains only what may ADVANCE at committee (steps 9–10) and never skips/delays/shrinks the swarm; an empty Nomination Ledger from a skipped swarm is an INVALID run the auditor fails. (Note: the deeper root cause of the empty Ledger was ALSO no AQE export reaching the workspace — tracked separately as the Drive-pull fix; this decision closes the breach-skips-swarm half.) | The dry-run behaviour of standing the swarm down on a portfolio breach; the implicit "breach = no premarket analysis" reading |
| D-64 | 2026-07-21 | **Scheduled sessions now SELF-BOOTSTRAP their workspace + credential (PM: "So.. post market didn't run? … This is rubbish agentic system if it can't run jobs").** ROOT CAUSE, proven by a live fired-session diagnostic (`aegis/data/diag/bootstrap_report.txt`): a scheduled task fires a BRAND-NEW ephemeral container that has the aegis-v4 plugin (skills+agents) and all MCP connectors (FMP, Tiger, IBKR, Drive) — but NOT the `/home/claude/aegis` workspace, NOT `config/.env`, and NO usable git credential (env `GH_TOKEN` is a `proxy-injected` sentinel, `GITHUB_PAT` empty). So the old prompts ("read aegis/CONTEXT.md, run tools/…") failed at line 1 — nothing to read, no token to clone, no way to push — and the session died producing zero output/commit/page (SILENT DEATH — exactly the SCHEDULERS.md "last unknown: does a fresh session bootstrap?"; the answer was NO). FIX: every scheduled prompt now runs a **STEP 0 bootstrap** BEFORE the phase — clone the repo to `/home/claude/AQE` with an inline PAT, write `config/.env`, pin committer, `cd` in, verify `aegis/CONTEXT.md`, page+STOP on failure — then run the phase and `git_sync.py` push. New `tools/bootstrap.py` ships the same contract for reinstalled plugins (carries NO token — D-49 preserved; the PAT travels inline in the trigger prompt, the only channel a fresh container can read). Both core triggers rebuilt (v2) because trigger prompts are immutable (`prompt_update_disabled`). Also captured: Tiger MCP needs a ToolSearch retry before it is declared unavailable. SECURITY: the PAT now lives in the trigger config at rest — recommend rotating to a fine-grained, repo-scoped, contents-only PAT. | The unstated assumption that a scheduled fresh session inherits this session's workspace/.env/token; the broken v1 market-watch + post-market triggers |
| D-63 | 2026-07-20 | **Runner-gate + runner-conviction pod + email KEPT as the reconciliation reference (PM: "voices find high-conviction RUNNERS in momentum, not the perfect stock"; "extension above MA is strength, resistance depends where it is, risk is a bracket question not a gate"; "keep the email so I know what fired vs a silent death here").** THREE corrections: (1) the alert gate is now a **RUNNER gate** — strong-momentum universe + leader (rs) + moving; it drops noise (COTY rank158, off-universe) but NEVER filters a runner on perfection. Extension, resistance, R:R, detect-strength, knn ride as SOFT NOTES, not gate checks (extension is strength in a momentum book; risk lives in the bracket, D-52; odds are the PM's call). (2) The intraday pod evaluates an alert **exactly as the premarket swarm evaluates a candidate** — the voices' own methodology + normal conviction, ONE evaluation logic at SOD and intraday (anti-spaghetti). Correcting an over-swing: the fix is NOT a special "runner-conviction" mode with positive bias — it is simply to STOP distorting the voices. No perfectionist filter layered on top (extension/R:R/perfection = swing/position trading, not momentum) and no cheerleading bias under; the voices were chosen as momentum traders, so their native lens already speaks to the book (PBF: mp 100 / rs +66 / 41% extended — a leader the perfectionist filter would have killed). Perfectionist objections (extension, overhead, detect-strength) are bracket/PM inputs, never stand-downs. (3) **Email is RETAINED** (reverses D-62's drop-email): AQE keeps emailing what fired as the PM's ground truth; the agentic flow reads the same and produces a RECONCILIATION here (fired → gated → podded → surfaced/watched/dropped, each with reason) so nothing dies silently. `tools/alert_inbox.py` gate rewritten; `market_hours` pod rewritten. | The perfectionist quality-gate (D-62's extension/detect/knn hard checks) that hunted the flawless stock and would have filtered real runners like BILL; D-62's "turn the email off" |
| D-62 | 2026-07-20 | **AQE alerts deploy via an INBOX + deliberation, not email (PM: "capture AQE alerts instead of my email… based on today's strong-momentum universe… every 15 min… flashes out only if worth taking — deploy the design we discussed, not the old email way").** The correct pattern, now built: AQE engine writes alerts every 15 min to `aegis/data/alerts/DATE/inbox.jsonl` (`contracts/alert_inbox.schema.json`), **scoped to today's strong-momentum universe**, and STOPS emailing. The scheduled market-watch session sweeps the inbox with new `tools/alert_inbox.py` (deterministic): dedupe vs cursor → enrich from the day's export → PRE-FILTER (held-book stop/approach → page immediately; opportunity alert clearing sc_mom≥70 OR lens≥2/6 OR runner → pod-worthy; stale/off-universe/event-driven/low-composite noise → logged not podded, D-61). The 3-lens pod (Detect+Elder+fitting voice) runs ONLY on survivors and pages ONLY on a CONFIRM that is also gate-actionable — "flash if worth taking." `market_hours` step 1/1b rewritten; `parameters.yaml` alert_feed_minutes=15 / alert_scope=strong_momentum / sweep=15. **Validated 20 Jul end to end:** synthetic inbox of the real alert names → BILL/DINO/MAN + SLDE-stop routed correctly, COTY(rank158)/AAL filtered; BILL podded → 3/3 STAND-DOWN → no page (correct). AQE-SIDE change required (PM infra): write to the inbox scoped to today's universe, drop the email. | D-54's full-universe/30-min poll framing (superseded: scope not cadence solves noise); the raw AQE→email channel entirely |
| D-61 | 2026-07-20 | **Voice data-fitness turnaround audit (PM ask — "confirm voices have what they need per their trading principles").** Empirical check of every voice's menu against today's export population: momentum/structure/participation core (`sc_momentum`, `structure`, `flow`, `rvol`, `rs_spy_20d`, `elder`, `mp`) is 100% for all voices. THREE gaps found + acted: (1) **risk-definition starved** — `bracket.stop/rr/risk_pct` populate on only 14% (86% have invalid default brackets), so the risk voices (raschke ATR-stop, thorp RR) are half-blind; the D-52 bracket exercise MUST run on the deliberation set before the risk voices read — flagged as the #1 dry-run wiring item. (2) **dead menu fields** — `close/above_ema/vol_vs_30d` populate at 0% (absent from export); lynch read all three, seow two; remapped to real fields (`entry`, `sma_distance_pct`, `rvol`). (3) **thorp under-fed** — its `knn_prob` edge is 100% populated but the D-60 board ignored it; board's thorp metric repointed to `knn_prob`. Verified board↔menu mapping ALL green after fixes. | lynch/seow reading 0%-populated phantom fields; thorp board on the sparse `bracket.rr` instead of its populated `knn_prob` edge; the unmeasured assumption that a mapped menu = a fed voice |
| D-60 | 2026-07-20 | **Data-only sub-score board, attached to the consensus tally every day (PM: "much better way to see top picks — I want to see this everyday attached to the consensus picks").** New `tools/subscore_board.py` — DETERMINISTIC (law 4, no models, no tokens) — ranks the premarket universe three ways off raw AQE sub-scores: (1) detect lens alone (lens_positive), (2) each voice alone (its own menu fields, percentile-normalised), (3) a data-only TIERING by agreement across three ORTHOGONAL axes (voice sub-score breadth ≥3 · detect ≥4/6 · elder force ≥0.80 → Tier 1 = ≥2 axes). Wired into premarket step 6 (runs right after the tally) and the step-10 phone render, annotated against the nomination tally so the board↔consensus split is explicit — AGREE (data + votes), DATA-ONLY (board Tier-1 the voices under-nominated, e.g. EA/MTG on 18 Jul), CONSENSUS-ONLY (voted but not data-Tier-1). Transparency/decision-support only, NEVER a gate (D-4/D-52 spirit): it ranks, it never removes or advances; the committee still votes on top. The divergence is the point — where pure data and the sector/bellwether judgment differ is where judgment earns its keep. | The consensus tally standing alone with no raw-data cross-check; the one-off nature of the 18 Jul sub-score exploration |
| D-59 | 2026-07-20 | **Weekly bellwether letters are committee CONTEXT, never a gate (PM: "CS and Nick Crown does not gate — it is the bellwether the committee should quote as support or contradictory where appropriate").** The CS Weekly Market Overview and the Crown Macro Letter join the deliberation as context_only alongside SRM/macro (extends D-4 weather-not-gate). The committee reads them and QUOTES them where a name's read is genuinely supported or contradicted (apt or omitted, never decorative); no name is added or removed because a letter said so. `premarket` step 8 updated; ingest is a Weekly input. | Any reading of the macro letters as a screen/gate; leaving the letters as unused attachments |
| D-58 | 2026-07-20 | **Intraday watch is cheap-by-default (haiku), held-book-first, with an opt-in armed auto-execute (PM proposal).** The 30-min market-hours poll runs on a new `model_tiers.watch = haiku` tier — never opus on a routine cycle (anti-cost, anti-overfit, PM). Its PRIMARY always-on job is the held-book risk stream (SL hit/approaching, TP→scale+trail, ratchet) — Thorp's durable-EV point made structural; checked before any opportunity scan. Escalation to the judgment tier (the ≤3-lens quick deliberation, then the full pod) happens ONLY on PM request OR when autopilot's `auto_deliberate_execute` is armed. **Armed auto-execute fires only at consensus ≥ `auto_execute_consensus_min = 6` voices, within caps, via the staging-gatekeeper; below 6 it pages, never auto-fires.** `parameters.yaml` (model_tiers.watch, autopilot.auto_deliberate_execute + auto_execute_consensus_min) + `market_hours` steps 1a/1b. | Any implicit use of a judgment-tier model on the routine poll; the opportunity-scan-first ordering; leaving armed auto-execution unspecified |
| D-57 | 2026-07-20 | **AQE flag condition RATIFIED — replaces sc_momentum>70 (PM: "I agree to your replacement gate").** The rudimentary single-threshold flag is retired for a quality WATCH-SET built at SOD (structure + participation[rvol≥1.5 & flow>0 & rs_spy>0] + detect lens_positive≥4/6 + elder force + bracket-exercisable + extension penalty + sector-rotation alignment), with `event_clear` the ONLY hard gate and `sc_momentum/rank` demoted to tiebreak — everything else a signed score (D-4/D-52 doctrine). Live price then TRIGGERS (D-54); the two layers are separated (watch-set = who, static SOD; trigger = when, live). **Pardo guard honoured:** ship ONE robust profile now; the regime-adaptive/sector-rotation reweighting stays BEHIND walk-forward validation (no in-sample fit shipped). Filtered names stay VISIBLE (WATCH_B), never silently dropped — use all the data (PM). `parameters.yaml` `flag_condition:` block. Full spec = committee design round-table 20 Jul. | `sc_momentum > 70` as the flag; watch-set/trigger conflation |
| D-56 | 2026-07-20 | **git_sync self-heals a missing checkout + pins commit identity (PM: "that's on you to fix not me — you have all the paths and it never changed").** Root-caused the false "nothing to commit (clean)": with no `.git`, `git status --porcelain` returned empty stdout on a FATAL and the code read that as clean — a silent success, the exact anti-pattern. `tools/git_sync.py` rewritten: (1) resolves the git root via `rev-parse --show-toplevel`; if ABSENT (cold/ephemeral session) it SELF-HEALS — clones the repo fresh from `endpoints.json` `aqe_repo.url` with the token, mirrors the session's working state in, commits, pushes (a scheduled fresh session can now persist from a cold container); (2) hard-fails visibly if `git status` errors instead of reporting clean; (3) PINS committer identity to `Claude <noreply@anthropic.com>` (was `Aegis Kernel <aegis@local>` → the "Unverified" badge); (4) reads the remote from `endpoints.json` so it works with no local checkout. Order-blind, secret-safe, idempotent unchanged. | The silent false-"clean" on a missing repo; the unpinned/unverified committer identity; the human-in-the-loop framing of a bug that was always the kernel's to fix |
| D-55 | 2026-07-20 | **Voices DECLARE missing data; the Chief SOURCES it — data-driven self-heal (PM: "voices have to say if they're missing data… Chief orchestrator can then go source it with FMP or trigger AQE… sloppiness creeping in, not following the agentic loop").** A voice that finds a menu field absent/null must add it to `data_gaps[]` (declare, never work around, never invent — law 3); `nomination.schema.json` + `voice-common` output updated. The Chief then routes each gap by source per `field_dictionary.json` — market/technical fields (`ma_*`, `atr_14d`, `rvol`, `rs_spy_20d`, `sma_distance_pct`, `beta_30d`) from the **FMP MCP**; AQE engine fields (`sc_momentum`, `structure`, `mp`, `elder*`, `lens*`, `bracket`, `energy`, `flow`) from an **AQE re-export** — patches the record, and RE-RUNS the affected voice; only a genuinely-unavailable field proceeds with the gap noted; an un-healed declared gap is an auditor-flagged broken loop. `premarket` step 5 extended (complements D-53's mandatory taxonomy). **Also (PM):** `conviction_high` set to **7** voices (roster is now 11), retiring the stale "5 of 9". | Voices silently proceeding on missing fields (the 20 Jul half-blind swarm) with no sourcing loop; the stale `conviction_high: 5 of 9 voices` |
| D-54 | 2026-07-20 | **Intraday poll = full universe @ 30 min, with a two-path wake (PM correction of a wrong market-hours model).** The AQE alert engine polls the ENTIRE universe (not just approved names) every **30 min** (15 was noisy — PM) for breakout / stop-hit-or-approaching / approaching-buy. Rationale (PM standing principle): our SOD signals are not good enough to justify ignoring what the rest of the universe does intraday. Wake paths: **on-plan** mover → full re-validation + Intraday Review Pod (existing D-13); **off-plan** mover → a QUICK bounded deliberation (≤3 lenses: Detect + Elder + one fitting framework voice on the live record) that decides worth-PM-attention → page or log; off-plan names NEVER auto-stage (never PM-approved). `market_hours` step 1/1b rewritten; `parameters.yaml` adds `market_watch_poll_minutes: 30`, `market_watch_scope: full_universe`, `market_watch_triggers`; `SCHEDULERS.md` tightened to separate the scheduler (liveness + wake host) from the engine (the 30-min poll). | The "watch only the approved alert universe" tunnel-vision (old market_hours step 1); the unstated 15-min cadence |
| D-53 | 2026-07-20 | **Data taxonomy is mandatory for the swarm — a stripped universe is an invalid run (PM: "we build a data taxonomy for a reason and the agents skipped it").** The `universe.json` handed to voices MUST carry, per name, the full union of `VOICE_MENUS` sourced verbatim from the AQE `daily_list` record (elder_5d, elder_pattern, mp_state, structure_shift, sma_distance_pct, rvol, rs_*, ma_*, atr_14d, full bracket incl. valid/atr_fallback_stop/targets/rr, lens fields, sector_trend_state, beta_30d, composites). A universe reduced to a thin score summary is an INVALID run — the auditor fails it, the orchestrator rebuilds before spawning. Voices still read only their own menu (D-29), but the field must be PRESENT to be read. Root cause: the 20 Jul dry run fed an 8-field projection; voices nominated half-blind (they said so). `premarket` step 5 rewritten. | The implicit assumption that any universe file suffices for the swarm |
| D-52 | 2026-07-20 | **Bracket is NEVER a gate; an invalid default bracket triggers a BRACKET EXERCISE, not a rejection (PM standing ruling, restated after a dry-run breach).** When AQE's default `bracket.valid=false` (no structural stop auto-passed atr≥1.0 / rr≥2.0 / risk%≤ceiling), the name is NOT disqualified and the orchestrator must NOT report "no valid entry" (the 20 Jul dry-run error). Instead it derives a working bracket from the record AQE already provides — `atr_fallback_stop`, `bracket.targets` (prior-high / fib_1272 / fib_1618), structural `ma_*`/`fib_*` — computes risk%/RR, and carries it flagged `bracket: exercised`. Bracket quality stays a SOFT flag (reaffirms the existing step-10 rule + D-4 weather-not-gate doctrine). `premarket` step 10 rewritten. | My dry-run commentary treating `bracket.valid=false` as a blocking reason to hold — a violation of the existing rule |
| D-51 | 2026-07-20 | **Elder lens ACTIVATED as the 11th nominator (reserve activation, PM ask — "what happened to elder lens?").** Elder was benched in `voice-common` on the reasoning that his method is embedded in the AQE elder score; the PM's observation (continued strong `elder_5d` reliably precedes an upward burst in strong tape — a signal the single score flattens) activates the seat per the "activation = decisions_log entry" rule. New `skills/voice-elder-lens/SKILL.md` reads the elder_5d TRAJECTORY (sustained/accelerating force, persistence required — a single high reading is not enough), regime-scaled (lead in GREEN, demand a cleaner elder in YELLOW/mean-revert), orthogonal to detect-lens. Added to `VOICE_MENUS` (menu: elder, elder_5d, elder_pattern, mp_state, mp), `daily_flow_audit` VOICES, `voice-common` bench note. Roster now 11; Ledger proves/kills the seat like any voice. Rebuild required to compile the agent into the plugin. | Elder's reserve-bench status ("embedded in the elder score, not a standalone lens") |
| D-50 | 2026-07-20 | **Dry-run test pack (PM ask before go-live).** `tools/dryrun.py` — one command, GREEN/RED per layer, running the DETERMINISTIC core (kernel current, contracts valid, fund_config, historical store, GITHUB_PAT preflight, git push auth, /ops renders, notify cowork-native, self-heal doctrine); order-blind, read-only. `DRYRUN.md` — the runbook for the connector + agentic parts the session must drive (Drive/FMP/Tiger/IBKR live pulls; a supervised `/pm` that must produce a full 10-voice plan and STOP at previews; a gatekeeper no-consensus refusal; state persistence to GitHub + Drive). Explicitly NO ORDERS, autopilot never armed — the pack proves the system both RUNS and REFUSES to trade unattended. | (net-new go-live readiness tooling) |  (PM ask — "put it in the install pack, or a standing prompt to ask me for the key on a new session, else this repeats").** Hard rule: the `GITHUB_PAT` is NEVER shipped in the install pack (a secret in a distributable is a leak) — only `config/env.example` (the blank template) ships. Instead, new `tools/preflight.py` (read-only, order-blind) checks that the one local secret (`GITHUB_PAT`; brokers + Drive are OAuth connectors, no local secret) is present in gitignored `config/.env`, verifies the file is gitignored, and on MISSING prints the exact paste-one-liner. Wired as the FIRST action of every session via the CONTEXT.md bootstrap, and as step 0 of premarket + post-market — so a fresh/empty workspace asks the PM for the key ONCE and self-heals, rather than silently failing the post-market push. Because the Cowork workspace persists, the token is set once and stays; preflight is the safety net for a new deployment or a lost `.env`. | The silent-config-failure gap where a fresh workspace with no `.env` would fail to push with no prompt |  (PM correction: "PM-driven means I dry-run with high watch, the automation has to work").** Confirmed via the connector registry that NO GitHub MCP connector exists, so autonomous push cannot ride a connector (unlike Drive, which is a proper OAuth connector and is the connector-based data store). New `tools/git_sync.py` is the real push mechanism: it commits the shelf/state and pushes to `main` authenticating from `GITHUB_PAT`/`GH_TOKEN` in the environment (config/.env), used TRANSIENTLY in the push URL (never written to .git/config, never logged — token scrubbed from all output; verified 0 leaks). Order-blind (law 1 — syncs data only), idempotent (clean no-op when nothing changed), fail-visible (missing token → committed-not-pushed reported, never silently swallowed; the book/state is also safe in Drive). Wired into post-market step 1; `GITHUB_PAT` promoted from optional to REQUIRED in env.example + INSTALL Phase 3 (fine-grained, Contents R/W). This is what makes the unattended scheduled cadence commit its own results with the PM only supervising. | The human-in-the-loop assumption for git push; post-market's vague "sync/archive-commit" with no actual push tool |  Claude plugin agent names must be alphanumeric + hyphens only; `voice-detect_lens`'s underscore failed desktop install validation. Renamed the skill dir + frontmatter and the two source refs — `build_claude.py` (VOICE_MENUS key + menu-breach text) and `daily_flow_audit.py` VOICES — so the whole kernel is consistent; clean rebuild = 12 agents incl `voice-detect-lens`, zero underscore agent names. The rulebook `detect_lens_seat` flag is a config key (not an agent name) and is intentionally unchanged. Origin/main reconciled to match the installed plugin so the next build won't regenerate the broken name. | The underscore agent name that broke Claude plugin install validation |
| D-49 | 2026-07-20 | **Session preflight / self-healing secret prompt (PM ask — "put it in the install pack, or a standing prompt to ask me for the key on a new session, else this repeats").** Hard rule: the `GITHUB_PAT` is NEVER shipped in the install pack (a secret in a distributable is a leak) — only `config/env.example` (the blank template) ships. Instead, new `tools/preflight.py` (read-only, order-blind) checks that the one local secret (`GITHUB_PAT`; brokers + Drive are OAuth connectors, no local secret) is present in gitignored `config/.env`, verifies the file is gitignored, and on MISSING prints the exact paste-one-liner. Wired as the FIRST action of every session via the CONTEXT.md bootstrap, and as step 0 of premarket + post-market — so a fresh/empty workspace asks the PM for the key ONCE and self-heals, rather than silently failing the post-market push. Because the Cowork workspace persists, the token is set once and stays; preflight is the safety net for a new deployment or a lost `.env`. | The silent-config-failure gap where a fresh workspace with no `.env` would fail to push with no prompt |  (PM correction: "PM-driven means I dry-run with high watch, the automation has to work").** Confirmed via the connector registry that NO GitHub MCP connector exists, so autonomous push cannot ride a connector (unlike Drive, which is a proper OAuth connector and is the connector-based data store). New `tools/git_sync.py` is the real push mechanism: it commits the shelf/state and pushes to `main` authenticating from `GITHUB_PAT`/`GH_TOKEN` in the environment (config/.env), used TRANSIENTLY in the push URL (never written to .git/config, never logged — token scrubbed from all output; verified 0 leaks). Order-blind (law 1 — syncs data only), idempotent (clean no-op when nothing changed), fail-visible (missing token → committed-not-pushed reported, never silently swallowed; the book/state is also safe in Drive). Wired into post-market step 1; `GITHUB_PAT` promoted from optional to REQUIRED in env.example + INSTALL Phase 3 (fine-grained, Contents R/W). This is what makes the unattended scheduled cadence commit its own results with the PM only supervising. | The human-in-the-loop assumption for git push; post-market's vague "sync/archive-commit" with no actual push tool |  Claude plugin agent names must be alphanumeric + hyphens only; `voice-detect_lens`'s underscore failed desktop install validation. Renamed the skill dir + frontmatter and the two source refs — `build_claude.py` (VOICE_MENUS key + menu-breach text) and `daily_flow_audit.py` VOICES — so the whole kernel is consistent; clean rebuild = 12 agents incl `voice-detect-lens`, zero underscore agent names. The rulebook `detect_lens_seat` flag is a config key (not an agent name) and is intentionally unchanged. Origin/main reconciled to match the installed plugin so the next build won't regenerate the broken name. | The underscore agent name that broke Claude plugin install validation |
| D-48 | 2026-07-20 | **Autonomous GitHub push — no human bridge (PM correction: "PM-driven means I dry-run with high watch, the automation has to work").** Confirmed via the connector registry that NO GitHub MCP connector exists, so autonomous push cannot ride a connector (unlike Drive, which is a proper OAuth connector and is the connector-based data store). New `tools/git_sync.py` is the real push mechanism: it commits the shelf/state and pushes to `main` authenticating from `GITHUB_PAT`/`GH_TOKEN` in the environment (config/.env), used TRANSIENTLY in the push URL (never written to .git/config, never logged — token scrubbed from all output; verified 0 leaks). Order-blind (law 1 — syncs data only), idempotent (clean no-op when nothing changed), fail-visible (missing token → committed-not-pushed reported, never silently swallowed; the book/state is also safe in Drive). Wired into post-market step 1; `GITHUB_PAT` promoted from optional to REQUIRED in env.example + INSTALL Phase 3 (fine-grained, Contents R/W). This is what makes the unattended scheduled cadence commit its own results with the PM only supervising. | The human-in-the-loop assumption for git push; post-market's vague "sync/archive-commit" with no actual push tool |  Claude plugin agent names must be alphanumeric + hyphens only; `voice-detect_lens`'s underscore failed desktop install validation. Renamed the skill dir + frontmatter and the two source refs — `build_claude.py` (VOICE_MENUS key + menu-breach text) and `daily_flow_audit.py` VOICES — so the whole kernel is consistent; clean rebuild = 12 agents incl `voice-detect-lens`, zero underscore agent names. The rulebook `detect_lens_seat` flag is a config key (not an agent name) and is intentionally unchanged. Origin/main reconciled to match the installed plugin so the next build won't regenerate the broken name. | The underscore agent name that broke Claude plugin install validation |
| D-47 | 2026-07-20 | **`detect_lens` voice renamed to `detect-lens` (plugin-validation fix).** Claude plugin agent names must be alphanumeric + hyphens only; `voice-detect_lens`'s underscore failed desktop install validation. Renamed the skill dir + frontmatter and the two source refs — `build_claude.py` (VOICE_MENUS key + menu-breach text) and `daily_flow_audit.py` VOICES — so the whole kernel is consistent; clean rebuild = 12 agents incl `voice-detect-lens`, zero underscore agent names. The rulebook `detect_lens_seat` flag is a config key (not an agent name) and is intentionally unchanged. Origin/main reconciled to match the installed plugin so the next build won't regenerate the broken name. | The underscore agent name that broke Claude plugin install validation |
| D-46 | 2026-07-20 | **Pilot is Cowork-native; local services deferred to migration (PM correction).** The PM flagged that D-45's installer/config presented LOCAL services (WhatsApp, external watchdog, Windows Task Scheduler/batch — and by extension the OptiPlex box / Ollama / LiteLLM / Open WebUI / Kimi) as active, when the pilot runs on Claude Cowork and testing precedes any migration. Reassessed and re-pointed: `notify.py` default channel is now **cowork** — the run's message rides the scheduled-task NATIVE push (delivered `via cowork-push`, no external service); WhatsApp/Twilio/watchdog are gated OFF behind `AEGIS_NOTIFY_CHANNEL=whatsapp` as the POST-MIGRATION path. `ops_status.py` / `/ops` now report the cowork-native channel and no longer flag whatsapp/watchdog as PARTIAL on Cowork. `parameters.yaml` set `notifications.channel: cowork` + a `migration:` sub-block; `endpoints.json` marks the provider migration-only; INSTALL.md Phase 3/5/6/7 + `install_manifest.json` reframed (pilot = native push; watchdog/schtasks explicitly NOT used on Cowork); `NOTIFICATIONS_SETUP.md` banner-gated as the post-migration guide with a 3-line pilot section. The self-heal LOGIC (self_heal.py, /ops, /recover, notify.py) is harness-neutral and unchanged — only the channel binding moved. | D-45's implicit binding of the pilot to local notification/scheduler services — those are now migration-only |
| D-45 | 2026-07-19 | **Operational self-heal + phone notifications + on-demand `/ops` (PM ask — "this is a self-heal protocol; I'll always look to fix and to manually trigger critical steps").** The system now watches itself and can be fixed. Three additions, all owned by Engineering & Change, all ORDER-BLIND (constitution law 1): (1) `tools/notify.py` — a one-way phone-alert emitter (WhatsApp), with a deliberately UNMISTAKABLE failure page distinct from routine green pings (PM: all pings on, but a failure must never read like success), a T-minus-15 pre-run heartbeat on key runs, and an external dead-man's-switch check-in so the ABSENCE of a ping is itself the alarm; (2) `tools/self_heal.py` — the operational self-heal net: classify a failure (transient → bounded retry/reseed; structural → escalate with the manual fix; gate/tripwire → STAND DOWN, never healed), extends the D-40 data self-heal and reuses `historical_store`; (3) `tools/ops_status.py` + `skills/ops-status` (`/ops`) — the SYSTEM-liveness card (loops firing? state fresh? channels up?), sibling of `/status` (book), reusing `daily_flow_audit.py` (D-43). PM manual levers = `skills/recover` (`/heal /recover /repull /reseed`). Policy in `parameters.yaml` (`notifications:`, `self_heal:`); secrets in `config/.env`; wired as premarket step 12 + post-market step 5. Inviolable (the PM's "provided the agentic design is adhered to" condition): nothing in this layer places, sizes, or arms — a recovered loop that reaches execution still previews at the gate for the PM. | The silent-failure blind spot (a run could fail unseen), the no-on-demand-liveness-view gap, and the no-operational-recovery / manual-trigger path |
| D-44 | 2026-07-19 | **Plugin install is a hard desktop-only platform gate (evidence from a live install run).** A parallel session ran INSTALL.md Phases 0-4 against a real Cowork deployment end to end and cleanly — including an independent recompute of dynCap (66698.64) matching the account's recorded 66699 to the dollar. It then hit Phase 5 and confirmed by direct test: Cowork plugin installation cannot be performed from a remote/cloud execution container under any circumstance — no `/mnt/.local-plugins` or `/mnt/.plugins` mount exists there; the mechanism is desktop-app-only. INSTALL.md Phase 5 and `contracts/install_manifest.json` are corrected from an `[AI]+[PM]`-blended step to an explicit hard `[PM]` gate with an instruction not to attempt a workaround. Reconciled artefacts from that run merged into main: `config/endpoints.json` (aqe_repo.url filled, Cowork-native connector note added), `data/persistent/dyncap_ledger.json` (marked_asof note), new `data/persistent/install_state.json` (phases 0-4 passed with evidence, phase 5 blocked_pm). | INSTALL.md's prior assumption that Phase 5 was AI-executable with PM as a secondary/platform-wiring role only |
| D-43 | 2026-07-19 | **Daily agentic-flow audit — the flight recorder (PM ask).** The existing auditor (skills/auditor) checks INTEGRITY (valid files, sources, no fabrication) but there was no per-day record of what the agent hierarchy actually DID. New `tools/daily_flow_audit.py` reconstructs the day's flow deterministically from the shelf artifacts (no new per-skill instrumentation — anti-lasagna, D-27): per hierarchy layer (Chief → Research + 10-voice swarm → Committee-desk → Risk dynCap/VaR/gates → Execution gatekeeper → Engineering & Change self-heal/assurance → Operations journal/metrics) it marks touched/not-run + key outcomes + inferred spawn count + exceptions, writes `data/eod/DATE/flow_audit_DATE.json` and an HTML flight-recorder card. Wired into post-market step 3 (Engineering & Change assurance, alongside the integrity audit); the card rides the 10:00 morning summary to the PM's phone. A grey 'not run' layer is the signal — it surfaces a layer that should have fired but didn't. | The blind spot of no daily coverage record of the agentic flow (only per-session integrity, no 'what ran / what didn't' view) |
| D-42 | 2026-07-19 | **VaR is PARAMETRIC + PTJ read-location wired (PM ruling).** (1) The 1-month 95% VaR gate is computed the parametric variance-covariance way (single-factor/market model, z=1.645), NOT historical/simulated — new tool `tools/calculators/var_parametric.py`: portfolio VaR from per-name signed exposure (PTJ) × beta/total-vol (AQE export), market factor = SPY annual vol from the historical store, systematic (net-beta-dollar × market vol) + idiosyncratic (residual) decomposition, checked on the POST-ADD book → `var_ok` for gate_check; breach de-sizes new adds, never a blind refusal. RB:risk.gates.var_95_1m_pct_of_dyncap.method=parametric. Self-tested; indicative live-book VaR ≈16.3% of dynCap. (2) PTJ read/write LOCATION is unchanged from before (PM): Drive folder 15PR74ws_kTXTqCcEfRGga_jjHrMvbCEM, files `aegis_trade_journal_YYYY-MM-DD_PTJ.json` (written by the /ptj skill; the kernel only READS the latest) — recorded in RB:data_sources.ptj and config/aegis_fund.md `ptj_drive_folder_id`. (3) dynCap re-seeded from the REAL live PTJ (2026-07-17) via the new `dyncap_ledger.from_ptj` adapter: allocation 75000, cumulative realised −3,142.93, unrealised −5,158.43 → **dynCap 66,698.64** (was the PM's ~64k estimate). | The undefined VaR computation method (var_ok arriving as a bare boolean with no named model); the unwired PTJ read-location; the placeholder 64k dynCap estimate |
| D-41 | 2026-07-19 | **dynCap is MARK-TO-MARKET — current Aegis equity, not a static base (PM ruling; sets BL-030).** PM funded Aegis at 75k allocation, ~64k after unrealised drawdown, and chose that dynCap track current equity: `dynCap = allocated_capital + realised P&L (closed AEGIS) + UNREALISED P&L (open AEGIS, marked from the PTJ)`. Sizing and every risk gate now compute against what is actually held; it de-sizes in drawdown and recovers as the book does — procyclical by the PM's explicit choice. REFRESHED each premarket from the fresh PTJ (step 4, where the PTJ is already loaded — `dyncap_ledger.py update`), never a stale mark. Wired: dyncap_ledger.compute() now takes open positions + unrealised and adds them; dyncap_ledger.schema gains unrealised_pnl_usd/open_count/marked_asof; rulebook dyncap_method, sizing/LEAD desk docs, and aegis_fund.md prose all updated. **BL-030 SET: allocated_capital_usd=75000, dyncap seeded to 64000 (PM mark) pending first PTJ refresh.** Also set this session: RB:risk.gates — weighted-beta soft 2.0/hard 2.5, VaR hard 20% (soft 18), combined-stop/known-risk 12%, leverage kept 2.5/3.0. CRIT-1 stale-cache + fail-closed-on-unset guards preserved. | D-21's "dynCap = allocation + realised only; never recomputed from market value" (the static base that ignored unrealised drawdown); BL-030 as an open blocker |
| D-40 | 2026-07-19 | **Historical-store seeding is self-healing — who & when defined (PM question).** A ticker that surfaces without a monthly-store entry must be seeded from FMP automatically, not manually. TWO layers: (1) PRIMARY — AQE (Engineering & Change desk) maintains the store as part of its daily universe/export pipeline (it already pulls FMP for the whole universe; it creates/appends each name's monthly entry) — the durable home (BL-042). (2) SELF-HEAL — the premarket orchestrator (control plane, a deterministic TOOL call, NEVER a spawned agent — anti-lasagna) reconciles the deliberation-set + held book against the store at the TOP of step 9, BEFORE Risk/committee anchor on it: `historical_store.py check` → for each missing/stale name pull FMP `historical-price-eod-full` (MCP) → `historical_store.py seed`. WHEN: new name = one-time full backfill on first reconcile; monthly currency = a name goes stale once its last stored month predates the last completed month and is refreshed (lazy, ≈ monthly); too-new (<12mo) = recorded `deferred_too_new`, skipped daily, re-checked monthly (`--with-deferred`), auto-graduated once seasoned — never faked. FMP is the ONLY price source (matches the DoR mandate). `tools/historical_store.py` gains check/seed/staleness + shared resample-from-daily (single source of truth with the original ledger seed). | The store as a manually-seeded, silently-degrading dependency (a new ticker reaching committee with stats()=None and no trigger to fix it) |
| D-39 | 2026-07-19 | **Restore the voices' quality-evaluation flags dropped by drift (PM directive — restore, don't re-pick).** The 90+ AQE fields were under-used: the momentum-literature quality signals the voices ORIGINALLY asked for (squeeze/VCP coil, tight base, break-of-structure, impulse acceleration, RS leadership & down-day resilience, kNN edge, bull/bear divergence, overextension, ATR caution, CHoCH, impulse interruption) had been dropped from their data menus by drift. RESTORED, not offered as a menu: `tools/quality_flags.py` (deterministic, law 4) derives each named flag from the live record with its source anchor (D-20); `FLAG_VOICES`/`FLAG_CATALOG` map each flag to the exact frameworks that use it (single source of truth). Build injects each voice's OWN flags into its compiled agent (§2c) and puts the anchor fields back on its data menu (D-29 pattern). Flags are SOFT (D-37/D-38): stamped deterministically at premarket tally (step 6) alongside field_values, travel into the deliberation packet, weighed by committee-desk (a caution flag never auto-PASSes, a strength flag never auto-ADVANCEs; near-universal flags like rs_leader are table-stakes, absence of an expected strength flag is itself signal), and surfaced on the phone plan line in plain words. Thresholds = RB:quality_flags (PM-tunable). `exhaustion_score` deliberately EXCLUDED pending direction confirmation = BL-040. Also corrected a stale D-38 leak in committee-desk (bracket "validity" still worded as a hard ADVANCE gate → now: definable stop is the only bar, RR/ATR/risk% soft). | Voices reading a bare menu stripped of their own quality-evaluation vocabulary; the 90+ AQE fields sitting under-used; the D-38 hard-bracket leak in committee-desk step 4 |
| D-38 | 2026-07-19 | **Bracket validity is a SOFT quality flag, not a hard gate (PM chief frustration).** A high-beta breakout has a wide stop that mechanically fails the 2.0 RRR-to-TP2 test — but that is exactly the momentum setup we hunt, so RR/ATR-distance/risk%-ceiling must NOT cut it. Reclassified (RB:brackets): those three are SOFT QUALITY FLAGS surfaced at deliberation; the committee/PM weigh them and may advance. The ONLY hard floor is a DEFINABLE stop (structural, else AQE atr_fallback_stop) so dollar-risk is bounded, and R-sizing on the ACTUAL (wide) stop — risk is controlled by SIZE (fewer shares), never by refusing the trade. gate_check bracket_valid -> stop_defined; staging.schema check enum updated; gatekeeper refuses only on an undefinable stop or a portfolio breach. | Bracket/sizing validity as a hard gate that cut good breakout/momentum setups pre-deliberation (the PM's greatest source of frustration) |
| D-37 | 2026-07-19 | **Gate doctrine — few/late/named hard gates; everything else a soft flag that travels (PM chief complaint).** Fixes 'cut too early' + 'flip-flop 360'. RB:gate_doctrine now law: HARD gates (the ONLY cuts) = feed/tripwire stand-down, universe screen, regime RED, event filter (kept HARD per PM), bracket validity, hard portfolio breach, gatekeeper 7. SOFT flags travel WITH the name into ONE deliberation and remove nothing = SRM/macro, soft gates, lens warnings, near-miss list, single-voice conviction. Three early-cut fixes: (1) universe near-misses are SURFACED for revival (universe_screen.py near_miss band, RB:universe.near_miss) not silently cut; (2) a single voice at conviction >= RB:committee.solo_high_conviction_min (4) earns full deliberation, not the watch table; (3) event-driven stays hard by PM ruling but visible. ANTI-FLIP-FLOP: the committee deliberation is the ONE judgment point where all flags converge; no verdict reversal after it except the PM or a logged intraday pod re-review — context is an INPUT, never a gate applied after the decision. | Hard cuts before the holistic view (screen near-misses, lone high-conviction picks); judgment scattered across stages that let a name's status swing; context applied as a late gate that reversed verdicts |
| D-36 | 2026-07-19 | **Risk is a data-anchored VOICE in the deliberation, not a downstream calculator (PM clarification).** Risk's advice is not random and not a formula — like the 10 framework voices it ANCHORS on AQE data (ATR range, structural levels, macro, portfolio metrics) + tool scenarios, and it sits AT the committee table: post-tally it produces a risk-lens read per name (portfolio fit, sector exposure, ATR/structural risk, sizing conviction), committee-desk deliberates the framework case AGAINST that read, and the deliberated conviction flows BACK to anchor final sizing — TWO-WAY. The tools (sizing.py, book_sim.py, trailing_stop.py) are its data ANCHORS, not its identity. The risk lens rides committee-desk (judgment tier); the tools stay data-plane. Anti-anchoring preserved: the risk read is produced post-tally, never contaminating the isolated nominations. | The failure mode of Risk as an isolated flat calculator that never gets/gives input from the 10 deliberated voices; the one-way Research->Risk sequence for the judgment (the mechanical floors stay one-way) |
| D-35 | 2026-07-19 | **Deliberative, not a deterministic factory line — Risk advises with scenarios, simulation & a voice; agents communicate (PM directive).** Two corrections: (1) trailing tightens to just below a CLEARED structural level (20MA/fib) even before TP — a runner is protected on the way up (`trail_stop(cleared_levels=...)`, RB:trailing.structural_buffer). (2) Risk/bracket is NOT a one-layer calculator: it presents SIZING SCENARIOS (0.5R/1R/2R with a recommendation — its voice), and SIMULATES the post-action book (`tools/calculators/book_sim.py`: leverage/beta/sector-exposure/combined-stop before→after) for any entry/exit/scale/rotation. The PM interrogates and adjusts pre-approval — a conversation, not a one-way gate. The Chief carries the framing (what was dropped & why via watch table + /why, the analytical lens via charter, the logic sequence via routines) so the PM doesn't hold it in his head — that surfacing IS the cognitive-load reduction. Mechanical floors (never-lower stop, hard gates) still hold; the judgment above them is deliberative. | The over-deterministic 'Risk decides a number' framing; trailing that waited for TP/operative-stop and ignored cleared structural levels; the one-way-pipeline read of orchestration |
| D-34 | 2026-07-19 | **Exits are committee-deliberated & PM-decided, not mechanical (refines D-33).** PM: in the real process the committee DELIBERATES held positions in premarket — price vs ATR range, structural levels, scoring the stock (runners run, weakness → tighten) — and the PM decides partial-profit-vs-run, also weighing stronger rotation candidates and sector over-exposure. So D-33's fixed fractional scale-out was too mechanical. Correction: (1) committee-desk now produces `held_verdicts` (RUN/TAKE-PARTIAL/TIGHTEN/EXIT + bear case + anchors) — full deliberation, not a fold of voice lines; (2) the trailing stop stays the mechanical FLOOR (never lowers, protects regardless of judgment) but take-profit fractions are a SUGGESTED default — the PM decides partial-vs-run from the committee read, never auto-scaled; (3) rotation gains a third trigger, SECTOR OVER-EXPOSURE (RB:risk.gates.sector_exposure soft 25/hard 35% of dynCap), alongside thesis-decay and capital-competition. plan.schema held_actions gains committee_read + exit_reason SECTOR_OVEREXPOSURE. | D-33's fixed fractional scale-out as an automatic rule; held-book review as a mechanical byproduct rather than committee judgment; rotation without a sector-concentration trigger |
| D-33 | 2026-07-19 | **Unified position lifecycle — exits & trailing stops are first-class (PM directive).** The book exits (stop / take-profit / rotation) and trails stops, not just buys. Entry and exit are now symmetric: Risk DECIDES (size/stop/trail/scale/exit), Execution's gatekeeper PLACES — a two-step pipeline in both directions, orchestrated by the Chief adopting Risk then Execution. **The held book is priced/exit-checked FIRST every premarket, before new ideas are sized** (freed/protected capital sets available dynCap). New trailing-stop engine (`tools/calculators/trailing_stop.py`, RB:trailing, Risk-desk `exits` skill): hybrid = breakeven after +1R, then ratchet to AQE's fresh operative stop, milestone locks after TP1(≥entry)/TP2(≥TP1), NEVER lowers (ratchet invariant), EXIT signal if structure reaches price. Scale-out: fraction at TP1/TP2, run the rest. Cadence: nightly post-market recompute + intraday tighten on target-hit. plan.schema held_actions gains SCALE_OUT + tp_level/scale_shares/exit_reason. | 'trailing per calculators' as a dangling promise (no calculator existed); exits as a scattered by-product of held-book review; the entry-only framing of the shortlist->execution loop |
| D-32 | 2026-07-19 | **Back to five desks — Engineering & Change is one desk (reverts D-28's six-way split).** PM ruling: keep five desks, do NOT omit the change/engineering/tooling side — fold it into the fifth desk rather than splitting into Assurance & Governance + Change & Technical. The fifth desk, **Engineering & Change**, owns the whole technical+change+tooling function: AQE (MDS) + the new historical data layer, tools & calculators, data utilities, the 5 bench lenses, audit/scorer/learning, governance, and the managed-change pipeline. Build-vs-assure separation is provided by the PM approval gate (D-8), not a sixth desk (anti-lasagna applies to desks too). | D-28's Assurance & Governance / Change & Technical split; the six-desk structure |
| D-31 | 2026-07-19 | **Interaction layer is Claude Cowork — grounded, not assumed.** Verified from Anthropic's own docs: Cowork is a PERSISTENT workspace whose files sync across devices (start at desk, continue on phone); scheduled tasks run remotely with the app closed, each as its own Cowork session, managed from the mobile "Scheduled" sidebar; Claude pushes decision prompts to the phone when input is needed; paid plans (Max first). This resolves the UX "no phone read path" gap (G1): the Aegis kernel + data/ live in ONE Cowork workspace, every scheduled phase is a fresh session reading/writing those persistent files, and the PM's cockpit is the Cowork chat in the same workspace — a fresh chat reconstitutes from the persisted files (CONTEXT + charter + today's shelf), so live plan/book/armed-state are reachable on the phone. Precise SGT timing uses the create_trigger MCP (cron) rather than Cowork's built-in cadences. Also lands the QA-driven money-safety fixes (CRIT-1/HIGH-1/HIGH-2/MED-1/2/3/LOW-1). | The whole "does the interaction layer even work on the harness" uncertainty; G1's undefined phone read path; `/fa` and `/status` rendering "from conversation state"; the Kimi Tiger write surface | 
| D-30 | 2026-07-18 | **Kimi stays read-only until explicit production migration.** PM ruling, refines D-19: the Kimi deployment is analysis/shadow only — its package ships NO order path (no staging-gatekeeper, no broker write credential, no /arm) until the PM explicitly confirms migration into production. Only then is the order path added. Resolves BL-023 (no read/write broker credential question for Kimi now — it's read-only market/analysis data only). Claude remains the sole order-capable deployment; the one-armed-at-a-time invariant is moot until Kimi is promoted. | D-19's "Kimi carries the same order-path requirements as Claude"; BL-023's open credential-scope decision |
| D-29 | 2026-07-18 | **Voices must understand AQE data, not read it blind (anti-hallucination).** PM concern: the worst failure is a voice narrating fake analysis over numbers it doesn't understand. So the AQE bottoms-up build (D-24) must ship, per field: definition, unit/enum, AND how it is consumed/applied. Tonight: AQE's own 60-entry field glossary is captured into the kernel as `contracts/field_dictionary.json` (one place, from the live feed) and a Data Dictionary & Taxonomy Bridge maps every field to the voices that read it and how each framework applies it. Rule strengthened in voice-common: a voice may cite a field ONLY if it can define and apply it in its own framework terms; the orchestrator injects each menu field's definition at spawn; blind number-recital is a breach the auditor checks. Build-time injection of definitions into each compiled voice = BL-034 (tomorrow). | Voices reading a bare data menu with no field meanings; "reciting a score is not analysis" as guidance without an enforced dictionary behind it |
| D-28 | 2026-07-18 | **Change & Technical desk added; Engineering refocused to Assurance & Governance.** PM flagged that no desk owned AQE, the tools, or the utility build. New sixth desk **Change & Technical** owns: AQE as the Master Data Service (D-24) and the Aegis-side data contract with the separate AQE build track; the tools & calculators; the data utilities (janitor, migrate, etc.); the four technical design bench lenses (technical / indicator / data design + business process engineer); and it IMPLEMENTS approved changes (branch → verify → ship). The old Engineering desk becomes **Assurance & Governance**: audit, performance scorer + criteria, learning, governance of the managed-change pipeline (gates, retire-to-add), PM steer — and it holds the fifth bench seat (governance management). Separation of duties: the desk that BUILDS (Change & Technical) is not the desk that ASSURES/GOVERNS (Assurance & Governance); the PM approval gate (D-8) sits between them. This is the exact control against the unmanaged-change spaghetti this system exists to fix. Six desks total. | The Engineering desk as a build-and-assure catch-all; AQE/tools/utilities as unowned; the 5 bench seats sitting in one undifferentiated desk |
| D-27 | 2026-07-18 | **Layer rationalization — spawn only when isolation is the point (anti-lasagna).** PM mandate: too many spawned-subagent layers is worse than spaghetti. RULE: default every capability to a SKILL (instructions a persona runs inline) or a TOOL (deterministic Python); escalate to a spawned subagent ONLY with a named isolation reason — (I) anti-anchoring independence, (II) pinned deeper model tier for hard judgment, (III) security isolation from untrusted input. Outcome: SPAWNED = the 10 voices (I), committee-desk (II), staging-gatekeeper (III, armed/order-capable), + on-demand (intraday pod reusing voices; a single bench seat or the event filter only when isolation is specifically needed). DE-LAYERED to skills: the 5 Engineering bench seats become one 5-lens review SKILL run inline (spawn a seat only for a deep/contested review), and auditor / performance_scorer / learning become skills, not standing spawned agents. Desks are ONE level deep (persona → workers); no sub-desks. Build stops compiling the 5 eng agent files by default (folded into BL-033). | Bench seats / auditor / scorer / learning as always-spawned agents; "spawn a subagent per capability" as the default; any deeper-than-one-level desk nesting |
| D-26 | 2026-07-18 | **Agent hierarchy — desks, not a flat swarm.** The flat 16-agent / 27-skill layout made one orchestrator micromanage everything (the heaviness the PM flagged). New structure: PM → Chief Orchestrator (the persona you talk to; owns clock + command routing + delegation) → FIVE desks (Research, Risk, Execution, Operations, Engineering) → worker agents. A "desk lead" is a PERSONA the Chief adopts for a phase (an instruction module it loads), not a separately-spawned agent — because sub-agents can't reliably spawn sub-agents on either platform (one-level nesting). Files: skills reorganise into `orchestration/` (the 5 temporal process routines) + `desks/<desk>/` (that desk's skills + worker cards); agent files stay flat in `agents/` for harness discovery, flattened at build. **Risk desk owns R-sizing / dynCap / portfolio gates / hedge** (backed by sizing.py + fund_config.py), independent of the Execution desk that places orders (separation of duties). Migration is a single controlled step (BL-033); brains (Chief + 5 desk personas + Risk sizing skill) built first, non-destructive. | The flat peer-swarm with no management layer; the process orchestrators as monolithic top-to-bottom scripts; sizing as an unowned bare tool |
| D-25 | 2026-07-18 | **Platform approach + low-ops constraint (PM ruling).** Claude is the invested primary — the PM is paying for it, accepts its closed-ecosystem/cost limits, and wants it made optimal to reduce cognitive load (the current mess is legacy of the old non-agentic design, now being remediated). Kimi is a PARALLEL migration candidate for lower cost + agentic harness, but migration does NOT happen until proven: the design works AND Kimi executes same-or-better across ALL of {infrastructure, user-interaction endpoints, agentic orchestration, output quality}. **Hard design constraint:** the PM runs a live book and has minimal capacity for technical/hosting workload — every design choice must minimise ops/hosting burden on the PM; a solution that adds infra the PM must babysit is a worse solution even if cheaper. | Any assumption that Kimi migration is decided; any design that quietly increases the PM's hosting/ops load |
| D-24 | 2026-07-18 | **AQE is a Master Data Service — contract inverted.** AQE stops being the thing whose output defines the schema; the schema (`contracts/aqe_export.schema.json`) is declared by Aegis and AQE rebuilds to conform. AQE's role = fetch → derive → push: it is the calculation authority and the data store, with no UX obligation to Aegis (human inspection only if wanted) — which is why the end state has an AQE agent as the query interface to the store. Missing-field protocol: a needed field absent from the declared object is deliberated (methodology + soundness), implemented in AQE's Python calc layer, added to the object, then served — never guessed, never silent. Handoff written for the AQE build track (AQE_MASTER_DATA_CONTRACT.md). | "Reverse-engineer the contract from whatever AQE emits"; AQE as an opaque engine rather than a declared-contract MDS |
| D-23 | 2026-07-18 | **Aegis sub-fund config is a plain editable MD.** Moved from a YAML block in parameters.yaml to `config/aegis_fund.md` (PM ruling: a simple file I can adjust manually or via the cockpit). Machine-readable front-matter + human prose; read through one accessor `tools/fund_config.py` (fail-closed if missing). parameters.yaml no longer holds sub-fund capital — one place only. | The sub_fund YAML block in parameters.yaml as the config location |
| D-22 | 2026-07-18 | **Interaction model fixed (PM ruling).** (1) Entry point: each deployment BOOTSTRAPS on its vendor's native app — the Claude app for the Claude deployment, the Kimi app for the Kimi deployment — no custom terminal/surface is built; opening the project loads CONTEXT + charter and the deployment is live. (2) Plan data is TIERED (D-20): compact 3–5 anchor numbers per name by default, `/why TICKER` expands to the full cited data + bear case + dissent. (3) Live status is BOTH: on-demand text commands (/fa /ap /watch) AND a rendered consolidated card via the new `/status`. | The open question of "what is the persistent interface"; any plan to build a bespoke terminal surface; /why as a not-advanced-only lookup |
| D-21 | 2026-07-18 | **Aegis sub-fund capital + PTJ as the Aegis source of truth.** Aegis is one sub-fund; its capital lives in one place (parameters.yaml `sub_fund`: PM sets `allocated_capital_usd`, dynCap = allocation + realised P&L on closed AEGIS trades). The **Aegis PTJ file is the source of truth for Aegis positions and fills** — it is already AEGIS-filtered, so open-position attribution is ALREADY DONE (closes the old BL-027). Held-book review reads the PTJ, not raw broker totals nor the AQE export's held_positions. Other strategies (Income Wheel, Protege9) are deliberately NOT modelled — sibling config files if ever onboarded, never speculatively. | AQE export held_positions / raw broker totals as the Aegis book; BL-027's "one-time attribution needed" (PTJ already is it); the assumption that a multi-strategy config must be built now |
| D-20 | 2026-07-18 | **No black box — every surfaced idea carries its data anchor.** PM ruling: nominations, committee verdicts, and the plan told the PM the *conclusion* but not the *data* behind it (esp. the Detect lens), leaving the PM no numeric anchor to decide on. Fix COMPLETES the existing `fields_cited` structure rather than adding a layer: the orchestrator stamps `field_values` (the actual numbers behind each cited field) at tally — deterministic, zero model cost; committee-desk carries `data_anchors` up per verdict; the plan render shows a plain-labelled "why (data)" line per name and explicit `lens_readings` wherever the Detect lens spoke. A verdict/idea with empty anchors when the data existed is now a breach. | Ideas/verdicts surfaced as prose-only conclusions; the Detect lens reaching the PM as an unexplained hunch |
| D-19 | 2026-07-18 | **Claude and Kimi are two independent deployments, not one coupled system.** PM ruling: treat each as separate with its own wrapper; "parallel run" means the PM runs both independently and stays vendor-agnostic, with identical requirements/expectations on each — NOT a primary + shadow that feed one merged output. Removes D-15's coupling (one-cockpit / Kimi-read-diff-only / Kimi-diff-in-the-summary / Kimi-order-blind). Shared KERNEL in git (one source of truth); isolated RUNTIMES (they never talk at runtime). One hard safety invariant retained: only ONE deployment may be autopilot-armed for live orders at a time, since both hit the same broker accounts. | D-15's "ONE cockpit — the Claude app, Kimi is read/diff only", the cross-vendor diff in the 10am summary, and "staging-gatekeeper ships only in the Claude package" |
| D-18 | 2026-07-18 | **Orchestration is autonomous judgment, not a script — D-16's wording corrected, mechanism unchanged.** PM ruling: characterising control-plane orchestration as "sequencing, not deep reasoning" undersold what it actually does — every exception-ladder call (retry vs degrade vs stand-down vs disarm-first, recognising an anomaly, choosing a named degradation path) is real autonomous judgment inside a bounded ruleset, not rote execution. RB:model_tiers.control stays a genuinely capable model (sonnet), not the cheapest tier available — the distinction from judgment plane is SCOPE (bounded-ruleset autonomy vs open-ended multi-framework deliberation), not capability. Interfaces (cockpit, paging content) fall under the same "capable, not rote" standard — a page or a morning summary is PM-facing judgment output, not a template fill. | D-16's "not deep reasoning" framing of control-plane orchestration (mechanism/tiering unchanged, only the characterisation) |
| D-17 | 2026-07-18 | **Trade labeling + three-strategy capital segregation.** Aegis is one of THREE strategies (Aegis, Income Wheel, Protege9) sharing co-mingled capital across TWO brokers (Tiger, IBKR) — each needs its own dynamic capital book. Aegis-internal order tagging (strategy_tag=AEGIS + broker, stamped on every staging-gatekeeper record) is law effective immediately. Broker-native tagging is BLOCKED — the reused Tiger/IBKR MCP tools expose no order-tag/remark field; Phase 1 (PM stages personally) covers this by PM manually tagging at submission until BL-028 (MCP passthrough) or a Phase 2 fix lands. dynCap and all RB:risk.gates apply to the AEGIS-tagged book only, never co-mingled broker totals — today's dry-run leverage/held-book findings (BL-024, BL-025) are reclassified PROVISIONAL pending BL-027 (one-time PM position attribution). | Any prior assumption that broker account totals equal the Aegis book; BL-024/BL-025 standing as confirmed findings rather than provisional ones |
| D-16 | 2026-07-18 | **Model tiering — three planes, three cost tiers.** Judgment-plane agents (10 voices, 5 Engineering Bench seats, and the new committee-desk deliberation agent replacing "single shared session" premarket step 9, plus the Intraday Review Pod which already reuses voice agents) are pinned to RB:model_tiers.judgment in their own compiled files at build time — never inherited from whatever tier the orchestrator session happens to run. Control-plane orchestration (the five process skills, gatekeeper relay) runs the economy tier. Data-plane tools/*.py remain plain code, no model, unaffected. New agent role: committee-desk (contracts/committee.schema.json, mandatory bear_case + recorded dissent). Kimi-side pinning noted unverified pending BL-026 smoke test extension. | "Single shared session" deliberation running at whatever tier the orchestrator happened to be on; uniform model tier across judgment and control work |
| D-15 | 2026-07-18 | **Dual-cloud engine topology + Kimi full parallel.** Both chat clouds run the full scheduled phases as engines: the Claude workspace (primary, sole order path) and KimiClaw (full parallel shadow, go-live gated by smoke test BL-021). The PC narrows to AQE feed server + intraday alert watcher, pushing every export and alert to GitHub immediately — the clouds' only fresh-feed source (BL-022). ONE cockpit stands: the Claude app chat; approvals, arming and steering happen nowhere else — the Kimi surface is read/diff only. Kimi may hold READ-ONLY broker data (credential scoping = BL-023); no order-capable credential ever enters the Kimi cloud; the staging-gatekeeper skill ships only in the Claude package. Claude-side pages ride the app's native push. | D-9's "PC runs all phases" (PC role narrowed, not removed); the Kimi-CLI-only deployment framing; ntfy as the Claude-side paging channel (stays for PC-originated pages) |
| D-14 | 2026-07-18 | **Engineering Bench + measured learning.** Five specialist seats (technical, indicator, data, process, governance-chair) own the learning loop's implementation glue: bench triage in Design & Review, pipeline stage ownership, weekly engineering session. PM STEER protocol formalised (FYI / DECIDE / PRE-FIX / POST-FIX; unanswered DECIDEs re-surface daily). Success criteria defined in parameters, evaluated EVERY post-market into a scorecard (PASS/WATCH/FAIL; 2 failing windows = bench review). Every voice gets bounded compounding memory (stats, open picks, standing lessons with evidence + expiry) injected at spawn. | Findings-to-fixes as an unowned gap; scoring without criteria; voices that never remember |
| D-13 | 2026-07-18 | **Intraday Review Pod + live sector pulse.** An alert fire re-convenes a bounded committee (nominating voices + macro + Detect lens) with live price, live bracket distances, the feed record, and a LIVE sector pulse from market data (new tool srm_live). Majority CONFIRM required before any gatekeeper request. Pod may only confirm or stand down — never add names, change brackets, or resize. | "The plan is frozen at 9pm" as an absolute (frozen for CONTENT, no longer for confirmation intelligence) |
| D-12 | 2026-07-18 | **Exception doctrine ratified.** One ladder for every failure (detect → retry-once for transient pulls → NAMED degradations only → stand down fail-closed → disarm first → always record → notify by severity). Refusals are outcomes, not errors. Two notification severities: page-now (money/safety) vs the 10am summary. MISSED_BY_FAILURE distinguished from MISSED-BY-DESIGN. | Ad-hoc per-tool failure notes as the only failure model |
| D-11 | 2026-07-18 | **Event filter runs post-nomination, pre-deliberation, on nominated names only.** Screening the full daily universe for news premarket was waste and wrong order (PM). Flagged names stay visible in the tally, marked EVENT-DRIVEN, cannot advance. | "Event filter before any voice speaks / on every universe name" (v2.x lineage + premarket step 4) |
| D-10 | 2026-07-18 | **Charter v3.0 declared historical.** The new constitution + rulebook + parameters + decisions log fully supersede it; every operational rule was captured from v2.2/v2.8/the skills, and the PM has re-ratified all contested points (D-1..D-9). No intake required. If it ever surfaces, it goes to charter/archive/ as history only. | The standing intake request for the v3.0 export |
| D-7a | 2026-07-18 | **/arm expiry fixed at 05:30 SGT** (next occurrence). Definitive — past US close in summer and winter, zero timezone/DST logic to get wrong. Command grammar adopted: /arm /disarm /ap and the command registry (charter/commands.md). | The 10:00 SGT hard-cap and sentence-style arming |
| D-9 | 2026-07-18 | **Local-hybrid topology.** The PM's PC runs 24/7 as the server (harness, AQE, gateways). If the PC is down, the process stops — fail-safe by design, "better than running amok" (PM). Private GitHub is the knowledge/context/data resilience layer with a once-daily archive commit. | Cloud-first assumptions; Drive as primary store (demoted to optional mirror) |
| D-8 | 2026-07-18 | **Development pipeline is law.** All change walks capture → PM approval → branch → verify/shadow → ship → remember (skills/development). No edits outside it except PM parameter tweaks via set_param. Items without a named retirement are schema-invalid. | Unmanaged change — the root cause of the original spaghetti |
| D-7 | 2026-07-18 | **Autopilot switch replaces the Phase-1/Phase-2 framing.** Default = PREVIEW always. The PM may arm autopilot (dated, logged, auto-expiring ≤10:00 SGT) for the set window; while armed, the Staging Gatekeeper — alone — may execute Tiger's place-preview→confirm two-step for APPROVED preauthorised plan names within caps (RB:autopilot). Any kill condition disarms first, asks later. | D-1's static phase framing; "PM stages personally" as the only overnight mode |
| D-6 | 2026-07-18 | Portfolio beta gate window = **β30d** (soft 1.8 / hard 2.0); β60d is monitoring colour only. Authority: Amendment v2.8 §6.3 (the latest ratified word) + PM confirmation 18 Jul. **FINAL — Charter v3.0 declared historical (D-10), no longer required.** | Charter v2.2 §0.5/§6.4 (β60d gate) |
| D-5 | 2026-07-18 | **Detect lens becomes the 10th nominator** — files its own top-10 like any voice, counted equally in the tally. Ledger will measure whether the seat earns its keep. | Charter v3.0 rule that lens counts may never enter the tally (Rule 5 / §0.13.7c) |
| D-4 | 2026-07-18 | **SRM/macro is weather, never a gate.** Sector posture informs sizing tone and deployment, removes no name. | Gate 2 "Sector Allowed" as a hard gate (Charter v2.2 §4.1) |
| D-3 | 2026-07-18 | **Universe = daily open screen** (US primary common stock; close ≥ any of EMA20/50/100; mcap ≥ $2bn; volume ≥ 30-day average). PM rationale on record: "if nomination is gone, the signal wasn't strong or persistent anyway." Ledger keeps tracking dropped names to the end of their 15-day window. | The fixed hand-curated ~605-name universe and the screener-freeze standing ruling |
| D-2 | 2026-07-18 | **Nominate 10, deliberate 2+.** Every voice files 10 nominations (all recorded in the ledger); full deliberation only for names with ≥2 nominations or lens top tier; the rest ship as a ranked watch table. | "All ideas are deliberated" (framework draft line) |
| D-1 | 2026-07-18 | **Overnight execution Phase 1 = pre-authorised conditional brackets** staged personally by the PM by 21:00 SGT; system monitors only. **Phase 2 (capped auto-stage, 0.5R max, 2/night) is built but disabled** until the PM activates it by explicit ruling after a proven shadow period. | The unresolved conflict between the autonomy window and "PM review needed" on live sizing |

Historical record (pre-restructure): Charter lineage v1.0→v3.0 and Amendments v2.6–v2.8 are archived under `charter/archive/` on intake; their surviving content lives in the rulebook.
