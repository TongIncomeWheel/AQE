{
  "phase": 6,
  "status": "in_progress",
  "checks": [
    {"name": "phase0_4_verified", "pass": true, "evidence": "connectors resolvable; fund_config 75000/66699; historical store 869 tickers; contracts valid; tripwires clean 177 rows; PTJ v2.9.6 12 open; dyncap 66698.64 cross-checks 66699"},
    {"name": "phase2_agent_count", "pass": true, "evidence": "clean rebuild: 12 agents (10 voice-* + committee-desk + staging-gatekeeper); 30 skills"},
    {"name": "phase2_selfheal_layer_D45", "pass": true, "evidence": "tools/{notify,self_heal,ops_status}.py + skills/{ops-status,recover} + contracts/{self_heal,ops_status}.schema.json present"},
    {"name": "phase2_notify_cowork_native_D46", "pass": true, "evidence": "notifications.channel=cowork; whatsapp/watchdog deferred behind AEGIS_NOTIFY_CHANNEL=whatsapp"},
    {"name": "phase2_detect_lens_rename", "pass": true, "evidence": "voice-detect_lens -> voice-detect-lens across skill dir + build_claude.py (VOICE_MENUS key + breach text) + daily_flow_audit.py VOICES; rebuild shows 12 agents incl voice-detect-lens.md, ZERO underscore agent names. rulebook detect_lens_seat is a config key (not an agent name) -- intentionally unchanged. Fixes desktop plugin-validation failure."},
    {"name": "phase5_plugin_installed", "pass": true, "evidence": "PM installed aegis-v4 on desktop; validation passes with the detect-lens rename. Old aegis-cic v3.1.0 superseded."},
    {"name": "flow_drive_crud", "pass": true, "evidence": "LIVE 2026-07-20T03:00:11Z: Drive READ ok (PTJ folder 15PR74..., latest aegis_trade_journal_2026-07-17_PTJ.json v2.9.6 visible); Drive WRITE ok (create_file succeeded, id 1k1cxfz...). Connector has read+write scope. No connector delete tool -- flow appends/creates dated files, never deletes, so not required. NOTE: create_file converts text to a Google Doc unless disableConversionToGoogleType=true; the /ptj skill already writes application/json correctly (existing .json files confirm)."},
    {"name": "flow_github_crud", "pass": true, "evidence": "READ (git fetch/clone public) ok, no cred. WRITE (git push) proven this session WITH a PAT. CONFIG: deployed autonomous/scheduled sessions need a push credential available (interactive PM-driven sessions can paste a per-session PAT). No GitHub MCP connector exists -- push is via git CLI + token."}
  ],
  "pm_gate": "GitHub push credential for autonomous runs is the one open CONFIG item (Drive needs nothing -- read+write both live-verified). For the supervised pilot where the PM drives, a pasted per-session PAT is sufficient. Then Phase 6 (create scheduled tasks with push notifications on) and a Phase 7 supervised smoke test.",
  "notes": "Detect-lens plugin-validation fix applied to origin/main so the source of truth matches the installed plugin (prevents the next build regenerating the broken underscore agent). Drive + GitHub CRUD live-verified 2026-07-20T03:00:11Z. A throwaway Drive write-test file (aegis_crud_write_test_DELETE_ME, root) was created to prove write scope -- PM to delete.",
  "ts_from_env": "2026-07-20T03:00:11Z"
}
