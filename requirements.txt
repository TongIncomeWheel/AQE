pandas>=2.0
numpy>=1.24
pyarrow>=12.0
requests>=2.28
python-dotenv>=1.0
streamlit>=1.30
matplotlib>=3.7
tzdata>=2024.1                # Linux needs this for zoneinfo("Asia/Singapore")

# Google Drive sync from the cloud (OAuth refresh-token-based)
google-api-python-client>=2.100
google-auth>=2.20
google-auth-oauthlib>=1.0
google-auth-httplib2>=0.1

# dev-only (not used by the cloud deploy)
pytest>=7.0
